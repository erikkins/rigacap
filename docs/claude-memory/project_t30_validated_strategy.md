---
name: t30-validated-strategy-jun5-2026
description: "LOCKED Jun 5 2026: the first honestly-validated strategy. 20x4.5% positions, ensemble entry, trail 30%, top-100 survivorship-free. Tier-2 held-out: 13.3% ann / 0.80 Sharpe / 14% MDD / no losing 2yr window / recent-2y +22.4%. Replaces ALL prior (mirage) numbers."
metadata:
  node_type: memory
  type: project
  originSessionId: daad830f-fb74-43b7-b3d5-fc34eed698de
---

**LOCKED Jun 5 2026.** First strategy validated on clean, survivorship-free, split-correct, per-period-WF data with out-of-sample (Tier-2) confirmation. This is the new base; everything before it was measured on corrupted data.

## ▶ RESUME (Jun 9) — FULL COPY SWEEP COMPLETE; only 2 deploy-sensitive items left
Entire site honesty sweep DONE + building (verified; residual "mirage" greps = SVG logo coords only). All committed LOCAL, ~50 commits ahead of origin, NOTHING pushed (Erik reviews before release). DONE: landing hero=B ("Institutional discipline. Individual price."), all mirage killed across landing (hero/Performance/FAQ/Edge); Edge reframed → diversification/risk-weighted/honesty (benefit-not-recipe per "don't give it away"); **/for-advisers** page (option C, DE-GATED: self-serve trial + opt-in model-delivery, NOT application-based); **track-record** page (9yr/14%/0.92/17% + per-regime resilience table, killed +160%/21.5%/675%/Cascade-Guard — but its equity CHART still fetches OLD backend WF data → fixed only by productionizing t30v); **Methodology** (de-mirage + de-recipe); **ALL blogs** (PITFWU flagship `/blog/honest-backtest` "We Found Our Own Backtest Was Lying" ADDED; #1 WalkForward full honest rewrite; #4/#9 fixed; #7 dead-links repointed; #8 PITFWU tie-in; #10 "7 Regimes Deep Dive" REMOVED file+route+import+index); **We-Called-It engine** pivoted (system prompt leads-with-discipline, new `discipline_win` type, default mix = trade_result+discipline_win, we_called_it now opt-in); "not yet live" swept everywhere → "live, record accruing" (engine IS live once shipped; only multi-yr RECORD pending). Fee math honest: 1% on $200k = $2k/yr vs $129/mo = $1548. 6 FT/Economist mocks in `design/rigacap-*.html`.
**Since-then progress (Jun 9 afternoon):** `/for-advisers` WIRED as real React route (`ForAdvisersPage.jsx` + App route + footer link). Methodology Execution-Timing reframed to EOD parity (prod is EOD-only since intraday tested −17pp on earlier strategies; NOT intraday-enhanced). CB-aware silent-cash detector (main.py:~1696, checks `circuit_breaker_state.is_paused("live")`).

### t30v PRODUCTIONIZATION — port BUILT (staged, dormant), cutover PENDING (Jun 9)
**Decision: productionize t30v AS strategy 6** (Erik). Architecture found: strategy 6 (T3) params = JSON in `strategy_definitions`; WF reads it (`fixed_strategy_id=6`); but LIVE model_portfolio used MODULE CONSTANTS (6/0.15/12), NOT strategy 6 → the parity gap. Inverse-vol sizing existed ONLY in backtester.
**DONE + committed local (all DORMANT — gated `vol_weight>0`, prod behaviour UNCHANGED until strategy-6 JSON flips):** in `model_portfolio_service.py` — `_annualized_vol_20d` (matches backtester.py:1469 exactly: last 21 closes→20 rets→sample-std ddof1 ×√252×100, default 30) + `_inverse_vol_mult` (matches backtester.py:747: (med/vol)^w clamp[0.3,2.5]) + `_load_strategy_params(db)` (reads active strategy 6, falls back to constants; pos_size accepts 4.5 or 0.045) + `_candidate_vols` (batched `market_data_provider.fetch_bars`, on-the-fly, no schema change). `process_entries` now uses max_pos/pos_size/vol_weight from strategy 6 + applies inverse-vol mult + stashes `_entry_volatility`/`_vol_mult` audit into `signal_data_json`. Math smoke-verified.
**CB DECISION REVISED: KEEP CB ON for t30v** (Erik's catch) — under wide 30% trail, 3 same-day stops = a REAL crash, so CB becomes a genuine crash-breaker (validated 0 fires in 2017-2026 = prod matches backtest; fires only in untested tail crash = defensive bonus). Do NOT disable.
**CUTOVER CHECKLIST (needs DB+data access; run WF jobs SINGLY — parallel WF exhausts prod DB pool):**
1. Read strategy 6's CURRENT params JSON → write t30v update (max_positions=20 / position_size_pct=4.5 / trailing_stop_pct=30 / vol_weight=1.0 / dwap5 / near3 / CB ON). Defensive merge (preserve existing entry keys).
2. **vol_weight PARITY FIX (found Jun 9):** prod WF service configures via `StrategyParams.from_json(strategy.parameters)` + explicit overrides (walk_forward_service.py:257, 832-868) — `vol_weight` is NOT in that path (only the research monkeypatch had it). So `vol_weight` (and conviction_tilt if used) MUST be added to `StrategyParams`/`backtester.configure` so BOTH WF service AND prod read it from strategy 6. Else WF≠prod.
3. TRAIL WIRING unresolved parity Q: `_get_regime_trailing_stop` (model_portfolio_service.py:132) falls back to const 12 unless dashboard has regime-adjusted trail — does t30v use regime trail-adj / dd-tighten, off what base? Resolve via parity run, NOT blind. (DO NOT change const 12→30 directly — loosens stops immediately, not dormant.)
4. **CHART REGEN (= launch gate):** track-record equity chart fetches `/api/public/track-record` → serves a stored WF sim's `equity_curve_json` (currently OLD 5yr; endpoint ~main.py:9901; generator `generate_track_record_chart` main.py:7133 stitches sims' equity_curve_json with best/worst band + n_sims per point). Fix = run t30v 9yr WF (16 windows) → stitch → cache as that endpoint's source. CANNOT ship honest 9yr copy next to 5yr mirage chart → blocks marketing launch. ONE t30v WF run does triple duty: validates + feeds chart + confirms numbers.
5. PARITY RUN: prod path vs t30v WF on a window → iterate til match. → smoke → off-hours deploy dormant code FIRST (safe) → flip strategy 6 params → t30v live.

**Other deploy-sensitive item still open:** signup segmentation field (Individual/Adviser) = DB SCHEMA CHANGE → migration-first (SQLAlchemy auto-SELECT outage risk); signup flow in a separate auth router (`app/api/auth.py`), `User` model at `database.py:594`.

## The config (t30)
- **20 positions × 4.5%** each (≈90% invested) — diversified, NOT the old 6×15%.
- **Ensemble entry** unchanged: DWAP > 1.05×, within 3% of 50-day high, uptrend (>MA20/MA50), vol≥500k, px≥$15. (near-3% confirmed optimal; loosening it only hurts.)
- **Trail 30%** (wide — let winners run; the edge lives in the right tail).
- No profit-lock (it binds and HURTS — caps the right tail).
- Biweekly rebalance, carry positions, top-100 survivorship-free universe (60d-avg-vol + min-price, per-period re-rank).
- Runner: `scripts/pitfwu_wf_periods.py` (reuses prod run_walk_forward_simulation, strategy_id=6/T3 base + overrides).

## Validated numbers (Tier-2 held-out, 10 off-grid start dates, 2y windows)
- **ann mean 13.3% / Sharpe 0.80 / std 5.4pp / MDD 14% (worst 20.9%) / EVERY window positive (+6.3% to +22.4%).**
- **Recent-2y (2024-06→2026): +22.4% / 1.34 Sharpe / 12.3% MDD — clears the North Star in the bull.**
- Costs negligible: 65-103 trades/yr, 0.3-0.9pp drag at 10-20bps (liquid top-100 names).
- t25's 13.6% was OVERFIT — collapsed to 11.4% held-out (non-monotonic trail sweep + std anomaly were the tell). t30 firmed up out-of-sample.

## MULTI-REGIME validated (Jun 5, longhist 2017-2026, 16 windows, 3 downturns)
- **ann 13.1% / Sharpe 0.81 / MDD mean 12.3% (worst 24.1%) / Calmar 1.07 / 94% positive / floor -2.2%.** MATCHES the held-out 13.3%/0.80 → robust across a decade, not a 2022 fluke.
- **COVID handled, not survived:** the window with the full -34% crash (2018-07→2020-07) = 12.6% MDD / +5% ann; windows starting into COVID = +21-29% / Sharpe 1.2-1.8. Diversification + 200MA filter + V-recovery did it. NO fast-crash circuit-breaker needed.
- Clears North Star on RISK (MDD≤20 mean, Calmar≥1); short on return (13 vs 20) + Sharpe (0.81 vs 1.0). Regime-dependence is in the RETURN FLOOR (chop windows +3-9%, trend windows +20-28%), NOT crashes — so regime work targets chop-return (subtle, overfit-prone), not crash defense. N=1-bear problem largely gone (3 downturns now).

## How we got here (the arc)
1. Entry has a REAL but THIN edge (median +0.5-2%/60d, hit-rate 52-58% vs ~50%, high N) — NOT noise. But it caps upside (avoids the lottery tail; that tail isn't reachable by entry tuning — near-high sweep refuted).
2. **Concentration was the disease.** Broad equal-weight captures the qualifier MEAN (~15-23% raw); 6×15% realized only 2.4%. Going 6→20 positions: 2.4%→9.7%, Sharpe 0.30→0.69, worst MDD 31%→20%, erased losing windows. Diversification is the single biggest lever proven.
3. Wide trail adds the rest (lets winners run). Trail is NOT a precision lever (noisy 18-40%); 30% is robust.

## What this KILLS (all mirages — do not cite)
- 50% / 33.6% / 19.3% "M3" numbers; Trial 37 "+240%"; the "26% split-adjusted" heuristic number; every pre-Jun-5 backtest figure. All measured on split-corrupted and/or survivorship-biased and/or single-window-cherry-picked data. See [[critical-backtest-pickle-is-not-split-adjusted-jun-4-2026]].

## Honest marketing frame
NOT "20% annualized." Something like "double-digit through the cycle, market-beating risk-adjusted, no losing 2-year window in clean backtest; stronger in trending markets." True + defensible under the publisher's exemption.

## ▶ RESUME (Jun 8 night) — strategy DONE, marketing reframe IN PROGRESS
Strategy fully locked (t30v = 20x4.5 / trail30 / vol_weight=1.0 / top-100). Analyst (3 rounds) rates 8.5-9/10; commercial thesis = sell RISK-CONTROL (drawdown halved 35→17 vs raw momentum) to SOPHISTICATED self-directed + RIAs, NOT CAGR to mass retail. $129/mo fine for that buyer. Hero metric = naive-vs-t30v comparison (factor decomp proved implementation edge is RISK-CONTROL not return: raw 12mo momentum 22.2%/0.67/35% → t30v 14.0%/0.92/17%).
- **DONE:** `frontend/src/LandingPageV2.jsx` — hero→headline C ("hard part isn't returns, it's staying invested"); KILLED mirage numbers (21.5%/+160%/+109%/20.4%/+8%-2022) in hero-stats + Performance table + 4 FAQ answers → honest 14%/0.92/17% + naive-vs-t30v table + "not yet live" + revised-down-for-rigor candor. All 11 sections preserved. **Committed LOCAL, NOT pushed (37 commits ahead of origin — research scripts + this; review before release/deploy).**
- **5 design mocks built** (`design/rigacap-t30v-*.html` + `-founder` + `-defensive-mode`): FT/Economist paper look (cream #F5F1E8, Fraunces serif, claret #7A2430). Landing(C), Track-Record(underwater drawdown chart), Headlines(A-E, picked C), Defensive-mode(CB-pause-as-trust dashboard widget), Founder(solo-as-rigor reframe).
- **REMAINING marketing sweep:** (1) Edge section reframe (still cites old seven-regime/ensemble → t30v real edges: diversification/inverse-vol/disciplined-exit + keep honesty pillar); (2) Founder section reframe (solo-as-rigor); (3) soften "seven-regime" in ValueProp+pricing; (4) OTHER PAGES still carry mirage numbers — TrackRecordPage.jsx, AboutPage.jsx, MethodologyPageV2.jsx, blog pages. (5) productionize t30v to prod model portfolio (start live clock). (6) "Silent Cash" detector → CB-aware (false-alarmed Jun 8; CB correctly paused after Jun-5 4-stop cluster, resumes ~Jun-15).
- Founder story HOLDS but reframe: solo-quant-as-RIGOR not as-genius; update 5yr→9yr survivorship-free; "Ensemble I ran privately"→rebuilt+revalidated.

## Levers tested (Jun 8)
- **CONVICTION SIZING — WINNER → "t30c" (validated Jun 8).** Tilt position size by momentum rank at entry (rank1 bigger, last slot smaller; exposure-neutral; `conviction_tilt` in backtester, _conviction_mult). tilt 0.5 multi-regime longhist (2017-2026, 16 windows): **15.6% ann / 0.88 Sharpe / 12.8% mean MDD / 27.8% worst / 94% positive** vs t30 baseline 13.1/0.81/12.3/24.1. = +2.5pp return +0.07 Sharpe, cost = +3.7pp deeper WORST-case DD (concentration deepens ordinary pullbacks, NOT crashes — COVID windows amplified to +31-32%). Tier-2 held-out CONFIRMED (improved OOS: tilt0.3=16.8/0.95/Calmar1.27, tilt0.5=15.6/0.91/worst18.1). Peaks 0.3-0.5, REVERSES at 1.0 (over-concentration). tilt 0.8 worst-MDD 31% (in-sample) = the one to AVOID. **t30c = t30 + conviction tilt 0.5 (or 0.3 for more return, MDD-tail tradeoff).**
- **INVERSE-VOL SIZING → "t30v" (validated Jun 8, arguably the BETTER base).** Risk-parity: down-weight high-vol names by (median_vol/vol)^w, centered at median (exposure-neutral; `vol_weight`, _vol_mult). vol=1.0 multi-regime longhist: **14.0% ann / 0.92 Sharpe / 12.0% mean MDD / 17.0% WORST / 94% pos** — FIRST variant to clear the North-Star worst-MDD ≤20% on the longhist. vs t30c (conviction): −1.6pp return (14.0 vs 15.6) but worst-DD HALVED (17 vs 28), Sharpe higher (0.92 vs 0.88). COVID windows held 9-10% DD (inverse-vol tilts away from vol exactly when it spikes). **Conviction & inverse-vol DON'T compose — opposing tilts cancel (conv0.5+vol0.5 → Sharpe 0.60). It's a FORK:** t30c=Growth (return-max, 28% tail), t30v=Conservative (smooth, 17% tail, higher Sharpe). Sharpe-1.0 quest came ~0.08 short but produced a risk-tier fork (better answer). Tier-2 on t30v running.
- **DISPLACEMENT — DEAD/exhausted (parked).** Bump weakest incumbent for strong fresh signal (PLTR-lockout fix). Tested margin / bull-bear gate (redundant) / trend gate / SAME-SECTOR. Same-sector CONFIRMED the whipsaw mechanism (sector-chasing churn: global 5.8%→samesec 11.6% net) — but best variant still loses risk-adjusted (Sharpe 0.68<0.72, worst-DD 27>24) and is DOMINATED by conviction. "Let winners run, don't churn" held under every angle. Code default-off. cadence/cooldown untested but same churn-reducer class → expected to converge to baseline.
- **BREADTH — NEGATIVE (top-100 confirmed optimal).** top-100 12.8% → 150: 8.1% → 200: 4.0% → 300: −0.2% (monotonic destruction). The liquidity-100 cutoff IS a quality filter; wider admits junk. Universe choice validated, not arbitrary.

## FINAL PICK: t30v = vol1.0 (Jun 8). Sharpe 1.0 held-out. OPTIMIZATION DONE.
vol1.0 (FULL inverse-vol) beats vol0.5: longhist 14.0/0.92/17.0-worst vs vol0.5 13.4/0.87/22.7-worst. CRITICAL: vol0.5 looked better held-out (16.1/0.99/16.2) but COVID-inclusive longhist REVERSED it — vol0.5's lighter tilt lets COVID DD hit 22.7% (>20% target); vol1.0's full tilt holds 17% through COVID. Crash-inclusive test corrected the calm-period test (need strong vol down-weighting in real crashes). **t30v final = 20x4.5 / trail30 / vol_weight=1.0 / conv=0 / top-100. ~14% through-cycle (15.6% held-out) / Sharpe 0.92-1.00 / Calmar 1.2 / worst-MDD ~17% robust / clears 3 of 4 North Star (short only on return).** t30c (conviction 0.5) = Growth alternate (15.6/0.88/28-worst). Analyst rates t30v 8.5-9/10. Honest gaps (analyst-confirmed): BACKTEST not live (provenance, dominant), momentum single-factor (no orthogonal alpha — news/sentiment all dead), missing 2008/GFC. Backtest hygiene DONE (survivorship-free, point-in-time/no-lookahead, Tier-2/anti-snoop, liquid top-100/costs-modeled). NEXT = ship: kill live 21.5% mirage→relabel backtest; productionize t30v→start live clock; design mocks.

## LEVER HUNT COMPLETE (Jun 8) — backtest maxed
Board: concentration 6→20 ✅ (the win) | conviction sizing ✅ (t30c, +2.5pp) | exit/trail dialed (30%) | displacement ⛔ exhausted | breadth ⛔ negative. **t30c is as good as backtest-side optimization gets.** Analyst friend's read (8/10 IF real/net/long-live): we're at his possibility #1 (rigorous BACKTEST, ZERO live) — the strongest backtest tier but still a backtest. Remaining confidence = LIVE TIME + deeper-history regimes (2008/GFC), NOT more levers. NEXT (not more knobs): (1) productionize t30c → start the live clock [highest-value], (2) kill live mirage numbers + relabel as walk-forward backtest [standing liability], (3) 2008-depth history build.

## Next (Erik's call Jun 5)
- **Build longer history FIRST, then regimes.** Regime-switching on N=1 bear (2022) is the boss-level overfit; needs multiple cycles. Existing PITFWU bars reach ~2016 → 2018-Q4 + 2020-COVID windows are FREE (just run earlier start dates). Pre-2016 (2008/2011) needs yfinance (Alpaca SIP stops ~2016). [[t30-validated-strategy-jun5-2026]]
- Open: total-return vs price-return (~0.5%/yr for div payers); universe breadth (top-100 vs 150).
