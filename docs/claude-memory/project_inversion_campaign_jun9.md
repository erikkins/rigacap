---
name: inversion-campaign-jun9
description: "Jun 9 2026 — Inversion campaign (naive momentum + minimal defense) REFUTED: 17 variants, none got daily MDD under 40%. Naive's TRUE daily MDD is −52% (race JSON's −30% is monthly-sampling artifact). t30v's 17.9% through the same 2021-23 factor unwind = the gate conjunction is the moat. Behavioral sim: t30v beats panic-SPY in every scenario, does NOT beat panic-naive. t30v-side ablation launched."
metadata: 
  node_type: memory
  type: project
  originSessionId: 701a2e93-33c0-4e85-a7bc-2cb9d9956d94
---

# Inversion campaign (Jun 9 2026) — can naive momentum (28.8% CAGR) be defended cheaply? NO.

Follow-on to [[jun9-strategy-reckoning-and-launch]]. Erik's call: keep the 14% window-mean copy live for now (low traffic; 10% continuous "too far from North Star for me"), run the research. North Star 20/20/1.0/1.0 still locked.

**Harness:** `scripts/inversion_campaign.py` — daily-resolution sim on PITFWU parquet (veneer universe/split-adjusted/delistings), continuous 2017→2026 lens, real turnover, net@10/20bps. Pre-registered params (trail30=t30v, DD-tighten 15→8=May 22, 200MA=literature). Results: `scripts/inversion_results.json` (17 variants, checkpointed). Run under `backend/venv/bin/python` + `AWS_PROFILE=rigacap` (system python lacks aiohttp). Bars disk-cached at `~/.cache/pitfwu_close/`.

## The two headline discoveries
1. **Naive momentum's TRUE daily MDD is −52% to −55%, not the −30% in portfolio-race.json.** The race's naive curve is monthly-resolution (one point per rebalance) and missed the Jan-29-2021 intra-month blowoff peak and the 2.5-yr unwind trough (Oct 26 2023). Race JSON + the animation MUST be rebuilt at daily resolution — current published comparison is too KIND to naive. (Race CAGR validated: my A_base 29.9% vs race 28.8% ✓.)
2. **The 2021-23 momentum-factor unwind is the defining risk event and it's INDEX-INVISIBLE** — SPY was healthy through most of it, so SPY-200MA filters do nothing (B/C: MDD still ~54%). The factor crashed while the market rose.

## All 17 variants (CAGR gross / net@20bps / Sharpe / daily MDD)
- A naive base: 29.9 / 25.2 / 0.84 / **54.8** (yearly: 2020 +83, 2025 +52; 2021-23 unwind −52)
- B SPY200MA rebal: 25.2/21.6/0.78/53.7 · C SPY200MA daily: 21.3/17.5/0.71/54.4 — **index regime blind to factor unwind**
- **D trail30 only: 34.5 / 26.2 / 0.98 / 43.1 — best offense in the space** (Calmar 0.80)
- E trail30+ddtighten: 23.2/11.6/0.83/45.7 (29x turnover!) · J top10 full defense: 11.5/5.5/0.51/**58.6 — defended WORSE than naked** — **DD-tighten/whipsaw lesson: any defense that mechanically re-enters during a factor unwind compounds losses** (May 22 DD-tighten validated on DWAP-gated entries, does NOT transplant to forced monthly re-entry)
- F 200MA+t30: 30.1/23.8/0.93/43.1 · G all three: 23.2/13.6/0.86/40.9 (best MDD of all 17) · H DD-halve: 24.0/21.4/0.84/44.1 · I top10: 29.2/24.3/0.78/57.6
- Round 2 (gates/factor-trend, hypothesis formed on same window): K factor-own-200MA trend: 17.4/14.4/0.63/**60.3** (whipsaw at blowoff top) · L hi250-90% gate: 13.7/10.5/0.64/49.4 · M ma50 gate: 18.3/13.3/0.64/65.4 · N L+t30: 15.0/11.6/0.70/42.3 · O M+t30: 24.6/17.3/0.82/52.3 · P/Q combos: 19.5/54.2, 12.2/43.3

## Verdict
**The inversion thesis is REFUTED: nothing in the naive+overlay space gets MDD below ~40%.** t30v went through the same unwind at −17.9%. Its "terrible exchange rate" (gave up ~19pp CAGR for ~12pp DD) is partially vindicated: simple tools CANNOT buy that protection at any price tested. t30v's full conjunction (DWAP×1.05 + near-50d-high 3% + ensemble score + biweekly + inverse-vol) keeps you OUT of a factor unwind in a way no overlay replicates — soft single gates (ma50, hi250-90) lose the return WITHOUT gaining the protection.

## Behavioral sim (on weekly race data — daily would be harsher on naive)
Panic-sell at 20-25% own-DD, various re-entries: **t30v never trips (17.9% max). Panic-SPY realizes 0.1-9.3% → t30v's 9.9% beats behavioral SPY in EVERY scenario** (the honest animation story). Panic-naive still realizes 10-21% — behavior does NOT rescue us vs naive on this decade. Also: t30v's longest below-peak stretch = 37mo (worse than SPY 24mo, ≈naive 36mo) — defense is "shallow but LONG underwater"; don't oversell comfort.

## t30v-side ablation (continuous 2017-2026, base ref 9.8%/17.9%) — `scripts/ablation_results.json`
`pitfwu_wf_periods.py ablate` (wf() now takes dwap_th/near_hi/mom_days; disable gates = −1000/+1000; _MOMDAYS hook in patched configure). Results:
- **no_nearhigh 6.7%/19.6% · no_dwap 7.5%/15.9% — each gate alone ADDS return** (removing hurts). BUT **no_gates (both off) 9.4%/0.75/15.1% ≈ base with BETTER risk** — gates only work as a PAIR; net contribution of the pair ≈ zero return, slightly worse MDD. (Caveat: no_nearhigh may be partly split-artifact suppressed — the gate masks unadjusted-split names.)
- no_volw 10.9%/20.6% — inverse-vol = honest small trade (−1.1pp CAGR for −2.7pp MDD). trail40 7.2%/22.4% — t30 is the trail sweet spot.
- **m250 (12-mo ranking in t30v frame) 10.7%/0.80/18.8%, m120_250 10.8%/0.80/18.9% — RANKING HORIZON IS NOT THE GAP.** Same universe, same 250d ranking: naive frame 29.9% vs t30v frame 10.7%. The ~19pp lives in the FRAME (MA20/50 trend quality filter, max_hold=60 force-close, vacancy-only entry/cash drag, biweekly cadence), NOT selection.
- The t30v frame's real DD-technology (vs naive's 52%): trend quality filter + vacancy-only entry (no forced re-entry into a falling factor) + trail + diversification — NOT the DWAP/near-high gates.
- hold_inf ≡ base EXACTLY (9.8/17.9) — **max_hold is a NO-OP in the WF path** (period carry re-seeds positions, resetting entry age; joins cb_tighten in the silent-no-op list — past TPE sweeps over it optimized noise).
- no_quality 8.0/17.8 · no_filters_at_all 7.7/18.6 — filters fully stripped, frame still ~8%. **Partial filter removal always hurts; only complete removal is neutral** (filters function as a redundant conjunction).
- **m250_no_filters 9.9/0.66/20.1 — THE DECISIVE RESULT: naive's exact selection (250d, no filters, same universe) = 9.9% in the t30v frame vs 29.9% in the naive frame. The ENTIRE ~20pp gap is FRAME MECHANICS, not selection/filters.** Prime suspect: NO INCUMBENT DISPLACEMENT (stale book vs naive's wholesale monthly rotation; cf [[wf-prod-incumbent-displacement-gap]] May 29: displacement ≈ 2× returns).
- **RUNNING at session end (Jun 10 ~midnight): m250_nf_disp0, m250_nf_disp10, t30v_disp10** — if disp0 ≈ 30%, displacement margin becomes a SINGLE DIAL traversing the whole frontier (t30v 10/18 ↔ naive 30/52); North Star = find where 20% CAGR meets ≤20% MDD on the dial, then per-window Tier-2. t30v_disp10 = the relaxed-product candidate. (Displacement is ALSO the WF↔prod parity gap — productionizing it was already roadmapped.)

## Jun 10 morning — trade-level forensic SOLVED the frame gap
Displacement configs refuted the disp hypothesis as stated: m250_nf_disp0 6.6/46.5 (imported naive's DD, NOT its return), t30v_disp10 10.0/28.1. Cleared more suspects: fixed −8% stop (ExitStrategyConfig defaults 0 in ensemble path), dd_tighten (strategy-6 DB row has the keys but StrategyParams doesn't consume them — OFF in WF), "246 errors" (benign per-period info), trail execution (close-only, matches sim). **Real-but-bounded bug found: min_price $15 applied to END-adjusted prices → NVDA excluded from entry until 2020 ($3.60 adj), TSLA until ~2020 — only 2-3 names/scan but THE names. Fix = min_price=0 in research bench (universe already enforces $15 point-in-time at selection).**

**THE ANSWER (diag trade dumps, `scripts/diag_*_trades.json`):** frame's naive-config 2020 = **+127.8%/13.3% MDD — BEAT the clean sim's +83%** (98 market_regime exits sidestepped COVID; regime filter is genuinely excellent in crashes). But 2017-19 = −1.3% (sim +49) and 2021-23 = −6.1% (sim +43): **death by displacement churn — median hold 5 DAYS, 440/623 exits 'displaced' @ mean −0.57% each** (margin-0 daily score noise: sell-what-recovers, buy-what-mean-reverts). The rare rebalance_exit trades that were allowed to ride averaged **+14.9%**.

**Architecture insight: the frame has NO low-frequency wholesale-rotation mode.** Vacancy-only (real t30v) = book stagnation ~10% CAGR; displacement = noise churn. Naive's 28.8% needs "rotate whole book monthly on a slow signal" — neither mode expresses it. Test running at session end: `carry_positions=False` = biweekly wholesale rotation in-frame. Configs: **m250_nf_nocarry** (equivalence proof, expect ~25-30%) + **t30v_m250_nocarry** (filters back on = THE candidate product shape: disciplined periodic rotation + regime crash defense). (Old "carry=false drops returns 50-90%" rule was measured WITH entry gates on — re-entry had to re-qualify through DWAP/near-high; doesn't apply to gate-off configs.) → ablation_results.json.

## Jun 10 ~11am — FINAL CORRECTED MAP (insurance-pricing runs landed; earlier "regime = whole story" was WRONG)
- **m250_nf_nocarry_noreg 12.3/0.69/24.7** — regime filter costs only ~3.5pp net in ROTATION mode (my 2019 "leak table" overstated: the 2 biggest leaks were span-edge analysis artifacts). Remaining ~16pp vs sim-biweekly-28.5% = **trail30 + mechanical re-entry whipsaw** (same theorem as E/J/DD-tighten: EXITS ARE CHEAP, RE-ENTRIES ARE EXPENSIVE). Compensation: MDD 24.7 vs sim 44.9.
- **t30v_noregime 0.8/0.13/36.6, 143 trades (~15/yr!) — THE BIG DISCOVERY: in CARRIED mode the regime filter is t30v's RETURN ENGINE, not a tax (+9pp).** Vacancy-only + 30% trail = almost no turnover → stale book, dead capital, full unwind exposure. **Regime flushes are t30v's ONLY book-refresh mechanism — its 9.8% depends on 2018/2020/2022 stress events to force re-ranking. In a calm decade t30v ≈ 1%-class stagnation. CRITICAL PRODUCT RISK: return generation is crisis-dependent.**
- Final continuous map: naive 29.9/−52 (never exits) · nocarry-noreg "trail-only rotation" 12.3/24.7 (honest midpoint, beats t30v return) · t30v 9.8/17.9 (crisis-refreshed) · t30v-noreg 0.8/36.6.
- **Next-program candidates (Erik's call): (1) deliberate refresh — periodic partial rebalance (quarterly replace bottom-ranked) or LOW-FREQUENCY displacement w/ margin (monthly eval — untested; disp10 was daily-eval) so return doesn't depend on crises; (2) re-entry discipline — stopped-out names ineligible N periods / until reclaim (attacks the ~10pp whipsaw); (3) regime re-entry speed (smaller win than first thought, ~3.5pp budget in rotation mode). All gated per-window Tier-2.**

## (superseded by above) Jun 10 ~9am — the regime-filter-only story
Final forensic chain (2019 nocarry): frame holds the RIGHT names (18-20/20 overlap w/ ideal momentum book), RIGHT prices (execution audit exact), FULL book (18.6/20), books reconcile to the dollar — yet booked +8.8% while its own names compounded +26%. Per-period leak analysis dated every missing dollar: **Jan-2019 in cash through the V-bottom (SPY<200MA), May/Aug-2019 whipsaw exits+late re-entry, Jan-2020 virus-dip exit. THE MARKET REGIME FILTER (settings.MARKET_FILTER_ENABLED, backtester ~line 1930) IS THE DOMINANT RETURN TAX: ~10-15pp in choppy bull years; saved ~45pp+⅔ DD in 2020 (+127.8/13.3 vs sim +83/COVID-DD).** Eliminated as costs: gates/trail/sizing/ranking/cadence (≈0 net — composite-ranked sim 29.8% ≈ pure-mom 29.9%; HOLD=10 sim 28.5% — cadence-robust), stop_loss (off), dd_tighten (off in WF), max_hold (no-op), execution prices (exact closes). Nocarry boundary lag ~2-3pp; min_price bug ~1-2pp pre-2020.
**Decomposition: naive 28.8% − regime premium (~dominant, lumpy) − book mechanics (2-4pp) − min_price (1-2pp) ≈ t30v ~10%. NO hidden lever exists in the gate/trail/sizing space — six weeks of past tuning was optimizing ±1pp noise.**
**THE next research program: regime RE-ENTRY SPEED** (cost is V-recovery lag, not the exit; faster reclaim rule — SPY>50MA / N%-off-low / breadth — keeps crash save, claws back whipsaw). Note backtester already has `regime_reentry_mode` ("smart re-entry") + `regime_cooldown_days` + `bear_keep_pct` knobs at lines ~219-236 — UNEXPLORED. Insurance-pricing runs (market filter OFF: m250_nf_nocarry_noreg, t30v_noregime) launched → ablation_results.json; their result = the return budget the re-entry rule plays with.

## Jun 10 afternoon — REFRESH PROGRAM (Erik-approved order: refresh → discipline → re-entry speed)
Tasks #6/#7/#8. Harness now has `_REFRESH_TOP_M` (patched run_backtest drops carried positions out of top-M at period start; dropped value→cash via existing carried_value path; NOTE dropped exits don't book trade records) + `_REFRESH_BULL_ONLY` (skip refresh when SPY≤200MA at boundary).
- **P1 unconditional refresh FAILED:** refresh60 10.7/26.9 · refresh40 7.8/32.9 · refresh30 9.1/36.0 (vs base 9.8/17.9). Return ~flat, MDD +9-18pp — refresh kept rotating into unwind bounces (forced-re-entry disease again). Stagnation was accidentally protective.
- P1b SPY-gated refresh FAILED: refresh60_bull 11.3/26.9 (return +1.5pp but MDD unchanged — the unwind is index-invisible, SPY gate can't see it). P1c FACTOR-gated refresh ALSO failed: refresh60_factor 10.5/26.5 (real gate, verified different trade count after a first run where the gate wasn't plumbed — purged bogus cache entries; lesson: identical trade counts between gated/ungated = lever not connected).
- **STRUCTURAL P1 INSIGHT: refresh MDD is not a timing problem. A continuously refreshed book = permanently fresh max-beta momentum book = full factor DD. t30v's stale book and its mild 17.9% MDD are the SAME phenomenon (decayed holdings drift low-beta and cushion crashes). Return and DD scale together with book freshness.**
- **MIN-PRICE BUG FIXED on bench** (`_patched_configure` forces min_price=0; $15 floor stays point-in-time at universe selection). All ablation_results.json entries before `t30v_base_FIXED` ran on the bugged bench — within-file comparisons consistent, new-vs-old NOT. `t30v_base_FIXED` = corrected continuous t30v baseline (the reckoning's 9.8% is biased LOW — NVDA excluded pre-2020).
- **P1d RUNNING (fixed bench): "exits cheap, re-entries expensive" as a strategy rule** — drop out-of-top-60 ALWAYS (drops are exits, safe), block NEW entries while factor < its 200MA trend (`_ENTRY_FACTOR_GATE` via _calculate_momentum_score→None; `_GATE_SUSPENDED` protects refresh-ranking calls from emptying top-M and dumping the book). Configs: t30v_base_FIXED, t30v_drop60_egate, t30v_egate_only.
- P1d drop60_egate FAILED (8.9/24.8 — drops remove the protective stale holdings that RECOVER post-unwind). But:
- **⭐ EGATE BREAKTHROUGH (Jun 10 evening): `t30v_egate_only` = 13.3% ann / 0.96 Sharpe / 17.4% MDD continuous (fixed bench) vs corrected base 10.2/0.75/18.6 — PARETO IMPROVEMENT from ONE rule: block NEW entries while the naive-momentum factor curve is below its own 200d MA. No drops, exits unchanged.** Mechanism: blocks the toxic vacancy-refills into unwind bounces; cash accumulates in factor stress, redeploys early-recovery. Continuous return now ≈ SPY (13.3 vs 13.6) at HALF SPY's DD (17.4 vs 32). Implementable in prod (track virtual naive top-20 daily, point-in-time 200MA). Corrected baseline note: min-price fix moved base 9.8→10.2/17.9→18.6.
- **EGATE TIER-2 VERDICT (10 held-out windows, scripts/egate_t2_results.json): WASH on window-mean** — gate 15.3%/0.96/12.6 (worst 17.0) vs base 15.6%/1.00/13.4 (worst 17.2); gate wins ann 4/10 (wins cluster on windows crossing a stress transition; loses on empty-book mid-bear + pure-bull starts). **The +3.1pp continuous lift does NOT replicate per-window AND does not reverse — the lenses disagree SYSTEMATICALLY: gate value only exists for an existing book crossing factor stress; fresh-book windows can't exercise it. METHODOLOGY NOTE: per-window Tier-2 is blind to book-state-dependent mechanisms — needs a continuous-segment validation design.** Honest status: helps the live continuous book (+3.1pp, N≈3 stress episodes), ~free across start dates, NOT v2-validated. Pre-2016 extension = decisive test. ALSO: fixed-bench base window-mean = 15.6%/1.00 — better than marketed 13.3/14%.
- **P2 COMPLETE — clean negative:** ban1 ≡ base exactly, ban2/ban4 noise. The near-50d-high entry gate ALREADY enforces stop-out re-entry discipline (a stopped name can't re-qualify until it reclaims its high — longer + smarter than any fixed ban). Whipsaw tax only ever existed in gate-stripped variants.
- **P3 COMPLETE — all four regime-filter perturbations WORSE than shipped (base 10.2/0.75/18.6):** smartreentry 8.8/0.65/22.1 (bear-rally fakeouts > V-recovery captures; verified working — 7 fires in smoke) · bearkeep50 10.7/0.71/22.7 (half flush = half crash exposure) · smart_keep50 10.1/25.6 · cooldown10 10.0/22.5 (slower also worse). **The full-flush + patient-MA200-re-entry design is at a LOCAL OPTIMUM.** Wiring lesson: walk_forward_service:873-874 OVERWRITES backtester.regime_reentry_mode/bear_keep_pct from its own params AFTER configure (the knobs existed end-to-end but the backtester never READ them — read-sites added Jun 10, default-off, prod-safe; cooldown via params-object mutation; cash_mode_day_count increment added). Debug chain for posterity: configure-probe → stomped attr → pass via run_walk_forward_simulation kwargs (lines 1192-1194).

## Jun 10 late night — PRE-2016 EXTENSION BUILT + THE 28.8% BENCHMARK FALLS
**PITFWU extended to 2005** (`scripts/pitfwu_extend.py`, 2096 symbols validated 408/413 on 2016-overlap, de-adjusted yfinance→as-traded verified on AAPL $633 May-2014; S3: bars_ext/ + calendar_pre2016 + *_ext panels; veneer `v.EXT` opt-in flag, env `PITFWU_EXT=1` in harness+inversion sim; SURVIVORSHIP-BIASED pre-2016 — label everything). 2008-06 universe sanity: C/GE/BAC/JPM/AIG/XOM ✓. Storage roadmap REVISED (Erik): migration target = promote PITFWU to prod-canonical (see [[storage-migration-roadmap]]).
**NAIVE MOMENTUM OVER 21 YEARS (2007-2026, survivorship-FLATTERED): 19.0% gross / 13.2% net@20bps / −57% MDD (trough May-2009). By era: 2007-2016 = 8.4% CAGR (2008 −45.7%, 2009 only +8.8 = the momentum crash, missed the junk rally), 2017-2026 = 31.3%. THE 28.8% BENCHMARK WAS A SINGLE-REGIME ARTIFACT.** Product economics restored: t30v's give-up vs naive-net ≈ 3pp for ~38pp less MDD (nobody compounds through −57%). All overlay conclusions replicate on 21y (D_trail30 20.2 gross/11.4 net/52.7 MDD; DD-tighten nets 1.4%). → `inversion_results_ext.json`.
**21-YEAR FINALS (2007-2026 continuous, `ablation_results_ext.json`):**
- **t30v base: 8.3% / 0.73 / −19.3% MDD** through GFC+2009-momentum-crash+2011+COVID+2022 (vs SPY ~9.8%/−55, naive-net 13.2%/−57). Calmar 0.43 vs SPY 0.18 vs naive 0.23. Lags SPY ~1.5pp for 1/3 the drawdown — the product thesis demonstrated across every major stress event, not asserted from one bull decade. (Survivorship flatters the BENCHMARKS more than t30v — regime filter was mostly cash through 2008.)
- **EGATE VALIDATED on 21y: 8.9% / 0.77 / −18.5%, 895 trades (vs 1128)** — Pareto improvement on BOTH continuous lenses (+3.1pp on 2017-26, +0.6pp on 2007-26; edge concentrates in factor-unwind regimes), wash per-window, never negative anywhere. VERDICT: real but modest — adopt-grade (principled, free, prod-implementable: track virtual naive-top-20 curve daily + its 200MA, point-in-time), NOT the North-Star unlock.
- **CAMPAIGN COMPLETE.** Decision queue returns to product: live copy (the honest 21y story is STRONG: "~9% through two crashes at −18.5% worst; the index lost 55% twice"), daily-resolution race rebuild → animation, beta email, t30v go-live ± egate, PITFWU→prod-canonical migration.

## PROGRAM VERDICT (Jun 10 night) — t30v is at a SHARP LOCAL OPTIMUM
~45 configurations over 2 days, every single-lever perturbation in every direction tested: refresh (4 variants), gates (5), ranking horizon (2), trail (3), sizing (2), displacement (4), carry/rotation (3), stop bans (3), regime re-entry (4), cooldown, hold caps. **ALL worse or flat except: (1) egate (+3.1pp continuous, Tier-2 wash, N≈3 stress episodes — pre-2016 extension decides it), (2) min_price bench fix (+0.4pp, correctness).** The product configuration is not arbitrary — it is locally optimal across ~12 tested dimensions. NEXT DECISIVE STEP: pre-2016 parquet extension (yfinance) — simultaneously validates/kills egate, tests naive's 28.8% against the 2009 momentum crash, and powers the race animation back-history.

## ⭐ Jun 10 EVENING — t30v IS LIVE (cutover executed 22:00 UTC) + site fully on 21y canon
**CUTOVER DONE (`scripts/t30v_cutover.py`, archive at s3://...archive/pre-t30v-reset-2026-06-10.json incl. AMD +80.68%):** all 81 model_positions deleted, all 3 state rows reset to $100k, strategy 6 → t30v (20×4.5/trail30/volw1.0/dd_tighten 0), **+ NEW strategy_adaptive_params row source='t30v_cutover' (CRITICAL: live trail reads the LATEST adaptive row via signals.py ~820, NOT the strategy row — the Apr-14 run5 row was silently serving 12% trail/4 positions)**, CB pause files cleared (CB stays enabled). **Day 1 of the t30v live record = Jun 11 4:30 PM scan.** Egate = fast-follow this week (factor tracker seeded from bench curve + gate SIGNALS not just book + shadow days; name it "factor health check").
**Site sweep complete:** all pages on 21y canon. KEY CORRECTION: continuous-lens 2022 = **−7.5%** (vs SPY −19.9) — the old "+8%, positive through 2022" was window-framing; Blog2022Story rewritten ("We lost 7.5%. That difference is the whole product."), zero-losing-years claims removed (2017/2018/2022 were losing years; 2018 −12.3% vs SPY −7.0 = the honest wart, shown deliberately). Crisis-year canon: 2008 −0.5/SPY −37.7 · 2020 +31.9/+15.2 · 2022 −7.5/−19.9. Adviser page rebuilt (worst-6-months table — 4 sat out in cash; 0.43 monthly corr; −1.0% avg in SPY down-months vs −3.9%; turnover/tax honesty). Race animation: human-first single act + nerve-range finale ($473k vs $206k–$1.02M/6-10 sales vs $210k–$399k), robot toggle footnote, drag-to-scrub, collision-aware labels. Erik has a family RIA to review the adviser page.
**Prod fixes shipped: (1) social-content deadlock** (content moved out of exit transactions, pg advisory lock — Jun 10 deadlock rolled back GOOG +5.6% close + 25 STR closes; moot post-reset), **(2) all-cash-day briefing** now renders in the dashboard empty state. Last-24mo canon (daily-res): RigaCap +32.0/2.20 Sharpe/3.76 Calmar/8.5 MDD vs SPY +19.9/1.18/1.05/19.0 vs naive gross +71.9/1.35/1.91/37.7.
REMAINING QUEUE: egate fast-follow → beta email rewrite (new numbers/voice) → social voice rework. Social templates' "not a backtest" phrasing flagged to Erik (defensible for live signals, awkward vs site language).

## Next after ablation
- If hold_inf jumps return materially → candidate = m250_hold_inf-style relaxed t30v → per-window Tier-2 (held-out starts) MANDATORY before believing it (ablation variants are shaped by this window).
- If frame can't be opened up → t30v ~10%/15-18% is the honest product; remaining lever = pre-2016 parquet extension (2008 GFC + 2009 momentum crash — where naive momentum historically died; would also flip the animation's ending).
- Rebuild portfolio-race.json with DAILY naive resolution before building the animation (current −30% naive DD is a sampling artifact; truth −52%).
- Erik engaged with all interim findings live (Jun 9 night session); copy stays window-mean-14% meanwhile.
