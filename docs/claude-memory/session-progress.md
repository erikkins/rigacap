---
name: session-progress
description: "Rolling snapshot of the current working session — accomplishments, in-flight work, key context for a fresh session"
metadata: 
  node_type: memory
  type: project
  originSessionId: b87c584c-343d-4a11-aca7-a450196570be
---

# Session progress — updated 2026-09-14

## 📣 SEP 14 — AD SERVING review (thread opening, nothing done yet)
Erik: "look at how our ads are being served?" I gave 3 angles + asked which: (1) ad-door traffic/funnel from OUR DB (/momentum + /should-i-sell: landed→scroll→offer→signup→account + in-app rate) — I can pull now, best post-fix conversion view; (2) Google Ads DELIVERY (impr/spend/CTR/keywords/quality) — NO Ads API wired, needs console screenshot; (3) conversion-tracking health (begin_checkout/GA4→Ads import gaps, stale $39/$349). Recommended starting #1. AWAITING Erik's pick. Tools to pull #1: worker {"parquet_query"} is DuckDB(parquet) not this — use admin endpoint /api/admin/pageviews/summary or worker db_read of page_views; sis_funnel/mom_funnel live in /api/admin/pageviews/summary.

## ✅ SEP 12 — Email/copy fixes SHIPPED (6af0bdc, deployed) + migration live
Erik flagged 5 inconsistencies + 2 asks. ALL committed/pushed as 6af0bdc (prior commits 8a9a91e etc.). Prod migration (email_unsendable cols) live. Manual Maximizer digest sent to erik@rigacap (success).
- **#5 exact-29:** "~29"/"about 29"/"roughly 29" → "29" everywhere (email_service, newsletter_generator prompt+data, tier_serving, maximizer_service, digest_v3, frontend TierBookView/App/MomentumPage). Hold is EXACTLY 29 days.
- **#9 "tape" (cardinal rule):** newsletter _call_claude now FINAL-CHECKs each section for \btape\b → discard+regen (3 tries, corrective nudge on retry, scrub-to-"market" fallback so it can never ship). Added `import re`. This week's LOCKED draft untouched (Erik: don't rewrite today).
- **#10 subject/body:** Preserver digest subject "N new" now = today's book BUYS (preserver_todays_actions.buys), matching body "Today's Moves" — was fresh-signal count (2 vs 1 SMCI). email_service send_daily_summary.
- **#11 bounce handling:** send_email intercepts permanent 5xx (552/550, SMTPRecipientsRefused) via _permanent_smtp_code() → no futile retries, records to _permanent_bounces + pop_permanent_bounces(). Migration LIVE: users.email_unsendable/reason/at. scheduler.send_daily_emails: skips email_unsendable; marks unverified>7d-past-signup as unsendable; after batch drains bounces+marks all + sends DAILY ADMIN SUPPRESSION REPORT (send_admin_alert to ADMIN_EMAILS, only when marks>0). OAuth signups auto-verified so not flagged.
- **#12 both tiers to Erik:** scheduler ALWAYS_BOTH_TIERS={erik@rigacap.com} → duplicates his subscriber entry with is_maximizer flipped → gets Preserver AND Maximizer daily.
- **▶ NEXT STEP (awaiting go):** AST-check scheduler.py, then git commit + push all (backend + frontend). Then deploy (~4min CI). Files: newsletter_generator_service, email_service, tier_serving, maximizer_service, digest_v3, database, scheduler (backend); TierBookView, App, MomentumPage (frontend).

## Session progress — updated 2026-09-10

## 💥 PRICING CORRECTION (Sep 11) — SnapTrade is $1/MONTH/user, NOT $1/day!!
- Erik misread the pricing all session; "how billing works" = **$1/user/MONTH** (~$0.033/day). ALL the "$1/day / $30/mo / 23-51% of revenue" cost-panic below is WRONG — real COGS ≈ **~1% of a $129 sub = a non-issue.** Everything built is still correct & good hygiene, just NOT urgent. IMPLICATIONS: connect→cache→disconnect gymnastics + round-robin-on-test = UNNECESSARY.
- **SnapTrade BILLING FACTS (from their docs):** billed for any user who successfully connected a brokerage AT ANY POINT in the billing period; failed connections not billed; DISABLED (disconnected) connections STILL BILLED — only DELETE (deleteUser) stops it, and deleting removes from FOLLOWING month's invoice (min cost = $1 the month they first connect; no same-day-delete escape). SnapTrade RECOMMENDS: reconnect flow for disabled + cleanup task to delete stale users before month-end = EXACTLY what we built (deregister uses deleteUser ✓, reconnect re-registers ✓, sweep = the cleanup task ✓). → SCHEDULE the sweep before month-end (daily fine) once policy set.
- **CONNECT POLICY (Erik leaning, Sep 11):** TRIAL + PAID get live connect, but TRIAL only if EMAIL VERIFIED (so we never pay $1 for bogus/gawker signups). Free-floor = mock. Admin = test. This UNIFIES with the email-verification feature scoped at session start (bogus-signup fix). BUILD NEEDED (verification doesn't exist yet): migration users.email_verified_at(+sent_at); JWT verify link in welcome email + resend endpoint; GET /verify-email?token=; connect gate = admin OR paid OR (trial AND email_verified); frontend unverified-trial→"verify to connect" resend modal; sweep keeps paid+verified-trial, deletes lapsed/unconverted. OPEN: verification scope = connect-only now (Erik leaning) vs also broader "verify in N days or drop to proof floor" cutoff later. NOT yet greenlit to build — was about to confirm scope.
- **🐛 PROD CONNECT 502 — ROOT CAUSE FOUND & FIXED (Sep 11):** erikkins@gmail connect kept 502'ing. NOT bad key (creds authed fine), NOT timing/async, NOT org-wide users. REAL bug via logs: `StringDataRightTruncationError: value too long for varchar(255)` at signals.py:4049. Enabling KMS made encrypt_secret emit "kms:"+base64(ciphertext) (~250-400 chars) > user_secret VARCHAR(255). Sequence: register SUCCEEDS on prod (creates df65de44) → row UPDATE to save encrypted secret TRUNCATION-FAILS → row rolls back → ORPHANED prod user w/ no saved secret → every retry = 400 "already exists" (symptom, not disease). First KMS write ever (old test rows were plaintext pre-KMS). FIXES: (1) prod DB user_secret→TEXT via run_migration (unblocks immediately, no deploy needed). (2) force-deleted orphan df65de44 on prod → prod users=0. (3) model String(255)→Text committed 4d66892. LESSON: this would've hit EVERY new connection (all KMS-encrypted now) incl paying users — caught on test acct. Verify-first paid off (Erik corrected my async-race guess twice; logs+listUsers gave truth).
- **KEY FACTS LEARNED:** test & prod keys = SEPARATE user namespaces (test users: rigacap-mirror-test-1, c5dfa2f1=erik@rigacap; prod users: none now). client_ids: test=RIGACAP-LLC-TEST-EKAKS, prod=RIGACAP-LLC-RRRPN (both real/distinct, prod authenticates fine). NEW diag/admin worker events (87c224d, use `in event` guard not .get): {"snaptrade_list_users":{env}} + {"snaptrade_admin_delete":{user_id,env}}. snaptrade_service.list_users(env) added.
- **▶ NEXT: Erik RETRY connect as erikkins@gmail** (orphan cleared + column TEXT) → should register fresh on prod, save secret, open portal. Report brokerage list + link real broker → I peek to confirm real holdings via prod.
- **✅ PROD CONNECT FLOW PROVEN (Sep 11):** after KMS/column fix, erikkins@gmail connect on prod WORKED (popup+register+portal). Chose Schwab → Akoya "Forbidden: Subscription unavailable" = Schwab needs PROD approval (SnapTrade note: "up to 2 weeks for Schwab after app profile"). Portal dropdown shows full ~50 brokers. NOT billed (failed connection = no charge). REMAINING for prod = Schwab (+others) enablement on prod key = SnapTrade dashboard/approval task, ~2wk wait; ensure app profile complete to start clock. Alpaca Paper "maintenance"=transient.
- **✅ REAL HOLDINGS VALIDATED end-to-end (test key):** peeked erik@rigacap → 28 real symbols across Schwab Rollover IRA + E-Trade ×2 + sandbox(dummy). AAPL/NVDA/MSFT/GOOGL/TSLA = the in-book momentum names; rest ETFs/bonds/crypto = outside universe. Integration mechanic 100% works.
- **✅ EMAIL-VERIFICATION SHIPPED (d87c1e2, deploying ~4min):** build green, pushed. Post-verify toast + ?verified=1 handler added, VerifyToConnect modal + admin ?preview=connect|verify done. Preview after deploy: as admin go to /app?preview=connect (subscribe teaser) or /app?preview=verify (verify prompt). Details below.
- **🔨 EMAIL-VERIFICATION BUILD (now SHIPPED — see above):** connect policy = admin(test) OR paid OR (trial AND email-verified); free-floor→subscribe mock, unverified-trial→verify-to-connect modal. Scope = connect-gate only (broader "verify in N days" cutoff deferred). Migration DONE (users.email_verified_at+email_verification_sent_at). BACKEND (all AST-OK, uncommitted): User model+to_dict(email_verified), security.create_email_verification_token (48h), email_service.send_verification_email, auth.py _send_email_verification helper+called in register, GET /auth/verify-email (stamps+redirects FRONTEND_URL/app?verified=1), POST /auth/resend-verification (authed, rate-limited), Google/Apple signups auto-set email_verified_at, verify link = https://api.rigacap.com/api/auth/verify-email?token=. signals.py connect gate updated (403 verify_email_required for unverified trial, 402 else). FRONTEND (App.jsx, uncommitted): hook canConnectBroker=isAdmin||active||(trial&&email_verified)+isUnverifiedTrial; connectBroker routes real/verify/subscribe; verifyOpen state; VerifyToConnect modal (resend btn→/auth/resend-verification); admin ?preview=connect|verify effect. **REMAINING: (1) add ?verified=1 toast+refreshUser handler (was editing toast renderer at App.jsx:6559 — add 'verified' green case) (2) npm run build to verify (3) git commit+push (4) tell Erik how to preview: /app?preview=connect or ?preview=verify as admin.** NOTE prod DB already has verify columns (migration ran) so backend deploy is safe. erikkins@gmail row already deregistered from test → next connect registers FRESH on PROD. Erik: login as erikkins@gmail → Mirror → Connect → report (a) does portal open [bad prod key would error], (b) brokerage list size (prod-approved lineup, may be < test's "all integrations"), link a real broker. THEN I run {"snaptrade_holdings_for":"erikkins@gmail"} to confirm prod key valid + real tickers, then disconnect/delete + confirm slot frees. $1 hits this month (expected test cost). PROD KEY still UNVALIDATED until this passes (pair set = SNAPTRADE_PROD_CLIENT_ID=RIGACAP-LLC-RRRPN; unconfirmed it's the real approved key vs placeholder — dashboard https://dashboard.snaptrade.com/api-key).

## 🔨 SEP 10 — SnapTrade cost-control lifecycle BUILT (all shipped; costs below OVERSTATED — see correction above)
- **Problem:** SnapTrade prod = $1/user/day (~$30/mo). connect gated on require_valid_subscription = TRIALS (no-card, 14d full access) can connect → billable; trial-abandoners never fire Stripe cancel webhook. Erik: paid-only connect + soft-delete (hate deleting data) + preview for non-paid.
- **DONE (migration FIRST, per rule):** (1) prod migration LIVE via run_migration worker Lambda — snaptrade_users +status('active'|'deregistered', default active)/deregistered_at/deregistered_reason, user_secret now NULLABLE. Verified via information_schema. (2) snaptrade_service.delete_user() = DELETE /snapTrade/deleteUser (stops billing; 404-idempotent). (3) NEW app/services/snaptrade_lifecycle.deregister(db,user_id,reason,apply=) — calls delete_user FIRST, then soft-status row + null secret + UserEvent('snaptrade_deregistered'); never raises; apply=False=dry-run. (4) SnaptradeUser model cols added + secret nullable. (5) connect endpoint: PAID-ONLY gate (user.is_admin() or subscription.status=='active' else 402) + reconnect fix (deregistered row → re-register fresh, flip back active). (6) holdings endpoint guards status!='active'/null secret → not connected. (7) billing.handle_subscription_deleted → deregister(reason='churn'). (8) main.py worker event {"snaptrade_reconcile":{"apply":bool}} = sweep of status='active' rows, deregister any whose sub isn't paid-active-valid; DEFAULT DRY-RUN. (9) FRONTEND: canConnectBroker=isAdmin||status==active; non-paid click → BrokerConnectPreview modal (claret/paper, R⇄bank motif, 8 broker tiles + "40+ more", Preview pill, "Subscribe to connect"→create-checkout plan=monthly, read-only line; NO 'SnapTrade' name; scrubbed button title). Both builds GREEN, prerender clean, backend AST OK.
- **PUSHED 56feb97** (lifecycle+preview) **+ 9c3f874** (dual-key routing) → deploying via CI. NOT yet live-verified.
- **DUAL-KEY (9c3f874):** Erik keeps TEST key for admin demos (5-conn cap), paying users → PROD key. Prod key = BRAND-NEW credential pair (separate clientId/consumerKey). Each userSecret bound to one key → env fixed at register, stored in NEW snaptrade_users.st_env col ('test'|'prod', migration applied, 2 existing rows='test'). snaptrade_service now env-aware (_creds/_sign/is_configured/_call + all fns take env); test creds fall back to unprefixed SNAPTRADE_CLIENT_ID/CONSUMER_KEY, prod reads SNAPTRADE_PROD_*. connect: env='test' if admin else 'prod'. holdings/disconnect/deregister use row.st_env. deregister refuses to soft-status if that env's key absent (no false-deregister-while-billing). Offline signing test passed (test↔prod isolated).
- **✅ ENV VARS SET ON BOTH LAMBDAS (ran scripts/set-snaptrade-prod-env.sh):** api 57 vars (prod pair + KMS), worker 60 vars (prod pair + COPIED test pair + KMS), 0 lost on each (DATABASE_URL canary + verify passed). Backups /tmp/snaptrade-env-backup-20260910-110809/. Script bug fixed (verify passed BEFORE/MODE as argv not env → moved before python3). Script reads creds from ~/.rigacap/snaptrade-prod.env (mode 600); SAFE MERGE (get→backup→python merge preserving all→update-function-configuration --environment file://→wait→verify). Script UNCOMMITTED (untracked) + now scrubbed of secrets.
- **⚠️ SECRET HYGIENE:** Erik pasted prod creds into the script's EXAMPLE heredoc (not the creds file) → prod CONSUMER_KEY surfaced in transcript via editor diff. I moved them to ~/.rigacap/snaptrade-prod.env + scrubbed script back to placeholders. RECOMMENDED (not done): regenerate the SnapTrade consumer key in dashboard + re-run script, since it got logged. Erik hasn't acted on rotation.
- **✅ KMS KEY CREATED + VERIFIED (Erik wants it):** key 147fae49-0adc-4cb0-b1b2-b91c0330f5ef, alias/rigacap-snaptrade-secret, rotation ON. Key POLICY grants rigacap-prod-lambda-role (both Lambdas share it) Encrypt/Decrypt/DescribeKey/GenerateDataKey — resource-based, NO IAM-role change, delete key=grant gone. Code encrypt/decrypt round-trip PASSED against real key (kms: prefix; plaintext passthrough for legacy 2 test rows intact). Script defaults SNAPTRADE_KMS_KEY_ID to this key.
- **GAP before prod key set:** paying-user connect → 503 (frontend swallows = silent no-op); admins work on test key NOW. TODO: add scripts/set-snaptrade-prod-env.sh to git; consider terraform-tracking the KMS key later.
- **DEPLOYED 0f8baac:** reconcile sweep now scoped st_env='prod' + admin-exempt (was flagging admin/test wrongly); NEW worker events {"snaptrade_free_test_slots":{apply}} (deregister non-admin ACTIVE test rows to free the 5-cap) + {"snaptrade_holdings_for":"email|id"} (admin peek: symbols+sources only, no secrets/balances). Dry-run gotcha: {} is falsy → guard skipped → mangum fallthrough; pass {"apply":false} not {}.
- **DRY-RUN FINDINGS:** all 3 active snaptrade rows are st_env=test (nobody on prod yet → $0 prod exposure). Prod reconcile flags 0 (correct). free_test_slots would free 2: wonderboy461 (gawker — turns out he only REGISTERED/opened portal, NEVER linked a brokerage → 0 holdings, no tickers) + erikkins@gmail (ERIK'S OWN paid account, legacy test). erik@rigacap.com=admin kept.
- **DECISIONS (Sep 11):** erikkins@gmail.com stays OFF admin (Erik wants a real non-admin, no-flags account for true UX testing) → free_test_slots HELD (would otherwise free it→relink→prod $1/day). Gawker = probably just "is it real?" skeptic (opened portal, never linked).
- **⏳ TODO still queued:** (a) holdings CACHING (each refresh=$0.05; frontend calls holdings EVERY mount=leak). Plan: cache holdings_json+holdings_at on row, serve cache ($0) default, hit SnapTrade only on manual Refresh + once post-connect, show "as of". (b) EventBridge daily schedule for reconcile + apply:true (deliberately not added).
- **✅ TEST-SLOT CLEANUP APPLIED (Sep 11):** free_test_slots apply:true → deregistered gawker(wonderboy461) + erikkins@gmail from TEST key; erik@rigacap(admin) kept. 4 test slots now open. erikkins@gmail cleared → next connect lands on PROD.
- **🔑 REAL-connect-for-FREE idea — RESOLVED DIRECTION:** SnapTrade docs + prod-approval email say: TEST key = "for all dev/testing", PROD = "pay-as-you-go, charges for connected users." → our dual-key split (admin/dev→test, paying→prod) is EXACTLY their intended model. Gate#1 GREEN (test returns REAL accounts). Gate#2 GREEN (5-cap CONCURRENT, delete frees). Gate#3 = DROP round-robin-on-test (against intended use → revocation risk on the key we also demo with). NEW gotcha: brokerages on TEST ≠ PROD (some need extra approval) → must learn our PROD brokerage list; free-connect on test then convert could break if their broker isn't prod-approved. So free connect, if done, must be on PROD (~$1/prospect) or use mock/manual.
- **✅ RECONCILE SWEEP SCHEDULED (Sep 11, terraform):** added aws_cloudwatch_event_rule/target/permission `snaptrade_reconcile` to main.tf → TARGETED apply (3 added, 0 changed/destroyed) to avoid pre-existing drift. LIVE: rule ENABLED, cron(0 8 * * ? *)=daily 4AM ET, worker input {"snaptrade_reconcile":{"apply":true}}. Safe: prod-scoped + admin-exempt + 0 prod connections now. ⚠️ TERRAFORM DRIFT exists (full plan=6add/3change/3destroy): calendar_rebuild perm to add, double_signals perm to destroy (Erik confirmed double-signals email RETIRED = intended cleanup), +3 changes — did NOT blanket-apply; someone should reconcile drift separately.
- **✅ SIGNUP FUNNEL FIXES SHIPPED (Sep 11, aa83002 + 8a9a91e):** (1) In-app-browser OAuth wall (top leak ~43%) — LoginModal.jsx now EMAIL-FIRST + platform-aware escape: Android intent:// → Chrome one-tap; iOS Safari instructions (••• → Open in Safari) + copy-link (no iOS programmatic escape exists). Escape URL preserves current path + ?signin=1 → auto-reopens modal on same ad door in real browser (localStorage doesn't cross browsers). ?signin=1 handler added to LandingPageV2/Momentum/ShouldISell/App. New events oauth_inapp_escape_click/open/copy. (2) admin.py funnel: "Reached Stripe"→"Started checkout (upgrade)" (both ad-door + auth funnels) — checkout_redirect only fires on explicit upgrade under free-first, so ~0 was misleading not a leak. Builds green, deploying.
- **(analysis notes) SIGNUP FUNNEL:** Screenshot Auth/signup funnel n=7 (tiny/directional). Biggest red = "In-app browser (OAuth blocked)" 43% (3/7). FINDINGS: (1) ALREADY handled — LoginModal.jsx isInAppBrowser() hides Google/Apple buttons in-app + shows email form + banner; logs oauth_inapp_blocked. But banner LEADS WITH NEGATIVE ("Google/Apple don't work…") → users bail instead of using email. (2) "Reached Stripe:0" = BY DESIGN (free-first retired card-at-signup) → vestigial/misleading funnel row, relabel/drop. (3) auth mechanics clean (0 SDK/backend errors). PROPOSED (awaiting Erik go): (a) rewrite in-app path email-FIRST/positive in LoginModal.jsx (highest leverage — social ad traffic is mostly in-app webviews); (b) fix vestigial Reached-Stripe row. Also: double-signals alert email retired (confirmed).
- **⏳ AWAITING ERIK:** (1) PROD KEY RECONCILE — prod access FORMALLY approved (email), dashboard https://dashboard.snaptrade.com/api-key. Is the pair Erik gave earlier (SNAPTRADE_PROD_CLIENT_ID=RIGACAP-LLC-RRRPN) the REAL prod key or a placeholder? If dashboard differs, drop new vals in ~/.rigacap/snaptrade-prod.env + re-run scripts/set-snaptrade-prod-env.sh. (2) paste "How billing works" (does same-day connect→disconnect cost $1? does disconnect stop future days?) — decides prod-free-connect cost ~$1 vs ~$0. (3) EMPIRICAL TEST: log in as erikkins@gmail → Mirror → Connect → lands on PROD → proves key valid + shows prod brokerage list + confirm disconnect frees. Erik does interactive OAuth; I peek after.
- To view the mock: admins can't (always real connect); use a no-card trial acct → Mirror → Connect.

## ✅ SEP 10 — SnapTrade prod key + bogus-signup / email-verify / tracking (advisory, in flight)
- **SnapTrade prod key (advisory only, no code):** "Describe your application" → gave READ-ONLY Mirror framing (read holdings/positions → alignment vs model book; NO trades/no fund movement). "Controls" form: recommended Q1 = commentary + suggested SECURITIES-to-buy/sell (ADD, product literally publishes named signals) + suggested portfolios; OFF = personalized (one model for all, keeps clear of advice + Mirror counsel gate) + AI-generated (signals are rules-based). Q2 = "info only" + "may manually place trades"; OFF = converted-to-orders + automated (no order routing).
- **Bogus signup (wonderboy461@yahoo.com, prob a gawker):** 552 mail errors on every send. Erik wants (a) real email enforced — cut off free access after 5-7 days if unverified; (b) can we track a repeat gawker who wipes cookies / never accepts consent? IN FLIGHT: launched Explore agent (a7f9a84afbfb83330) to map signup/verification flow, User model cols (email_verified? ip? ), IP/device logging, CookieConsent GA4 gating, email bounce handling (552/550 suppression), free/trial state machine + existing time-cutoff hooks. Awaiting agent → then give grounded recs. Prelim tracking view: cookies wiped + consent declined kills GA4/first-party; only server-side levers survive (signup IP logged regardless of consent — imperfect: VPN/mobile-rotate/shared; Stripe payment method only if they pay = moot). Email-verify cutoff piggybacks naturally on free-first spec (email = primary entitlement contact there).

## ✅ SEP 5 — Newsletter Maximizer time-stops + D7 ribbon offset
- **Newsletter logic (SHIPPED bcfbd6f, deploying):** weekly "Market, Measured." only counted PRESERVER stops (model_positions portfolio_type='live', reasons trailing_stop/stop_loss/regime_exit) → "0 stops" true for Preserver but blind to Maximizer. Maximizer breakout book's 29-day time-stops live in `tier_fills` (tier='maximizer', side='sell', reason='hold_exit'). Added a tier_fills count (additive, try/except), surfaced as a DISTINCT data-block fact ("scheduled clock exit, NOT loss-cut"), updated §01 + §03 prompts to keep the two exit types distinct. Erik: DON'T regen — this week's letter is LOCKED; fix is for NEXT week's generation (also fixes the §03 "0 stops" bullet auto). newsletter_generator_service.py.
- **D7 "first week graded" regime ribbon offset (FIXED in scratchpad build_drip.py):** year labels were left-anchored at each year's START boundary (text-anchor default=start, x=i*w) → sat at far-left of each block = looked offset. Fixed to CENTER each label under its year's SPAN midpoint (text-anchor=middle, x=(i0+i1)/2*w) + partial-year min-gap guard (skip if span*w<24). Mirrors frontend BlogSectorObservatoryPage.jsx year-axis convention. Regenerated ribbon_fixed.png, Erik approved. NOTE: drip still NOT productionized (build_drip.py is a scratchpad sample; carry this fix into email_service.send_onboarding_email when porting). Fixed build_drip.py saved to session scratchpad.
- **Newsletter design (dawn header):** Erik floated adding sun/moon+dawn image like the digest; my rec = KEEP text-forward (essay genre ≠ product-email genre; restraint = premium signal). Erik agreed ("looks like a high end newsletter"). Offered 2 optional light-touch family cues if ever wanted: (1) thin dawn-gradient hairline under masthead replacing the 2px solid rule, (2) small spire/eclipse logomark above kicker. NOT built.

## ✅ SEP 4 (44dcba8) — FreeProofView table alignment (Erik's morning bug)
- Both the "21-year walk-forward record" table AND "Recent catches" in components/FreeProofView.jsx used `flex justify-between` → middle value floated by left label/ticker width ("Raw momentum (no floor)" shoved its 13.2%/yr right; days-held wobbled by ticker length). Fixed BOTH to `grid grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)]` (label left / value centered / DD-or-return right), tabular-nums + whitespace-nowrap. Build green, pushed, deploying.

## ✅ ANSWERED — preview expired/noaccount view (Erik's ask)
- Already exists (admin-only): `?preview_state=free|active|trial|expired|past_due|canceled` (backend signals.py:2129; anything ≠ active → FreeProofView teaser). Use `/app?preview_state=expired` (+ optional `&preview_tier=maximizer`). True logged-out "no account" chrome = same FreeProofView body; log out / incognito to see the "Sign in" CTA variant. Offered to add a distinct `noaccount` state if he wants it while logged in.

## ⚠️ VIX BACKFILL — fork DIED mid-run (prev process exited), NOTHING landed
- Safety-checked: all_data.parquet INTACT (5018 symbols; ^VIX & ^GSPC still frozen at 2026-06-15; no partial write, no .bak). git clean (fork committed nothing).
- STILL TODO (awaiting Erik go): backfill ^VIX/^GSPC 2026-06-15→today into all_data.parquet + ongoing persistence (targeted index-row merge each scan, NOT full 600-sym rewrite=OOM). Root cause: PRICE_SOURCE=parquet skips export_parquet (main.py:1652); indices can't go in PITFWU (Alpaca) so they live only in frozen all_data. Live regime VIX is FINE (re-fetched to data_cache each scan). Only research/WF reading ^VIX from parquet is affected. Re-run INCREMENTALLY this time (keep Erik in loop), safety rails: backup→merge-preserve→verify. Tool: {"parquet_query":{"sql":"...FROM prices..."}} (DuckDB).

## ✅ EARLIER (live in prod)
- Morning Health "DWAP 590/591" squashed (^VIX excluded, 51086e1). Mirror tour → real portal + "Your move" de-buy-pushed (bd45dc8). Mirror go-live (first+default tab). Daily digest v3 both tiers live.

## ⏳ OPEN / QUEUE
- VIX backfill (above) — awaiting go.
- **Tour firing scope** A/B/C: current A=localStorage per-browser (fires for anyone unseen). B=per-account server flag. C=B+new-signups-only. Awaiting Erik.
- **Landing "The Mirror" section** (LandingPageV2.jsx ACTIVE at /) — prime new signups for eclipse default tab; extract AlignmentEclipse (App.jsx:1069, not exported+circular) → components/AlignmentEclipse.jsx OR static PNG.
- **Drips** — productionize redesigned 6-step onboarding into email_service.send_onboarding_email (D1/D3/D7/D12/D15/D22); samples scratchpad build_drip*.py; pattern=backend/app/services/digest_v3.py.
- Password reset + win-back surfaces.

## KEY FACTS / TOOLS
- Email tests → erik@rigacap.com (NOT ekins@cookma.com=this window's Claude login). AWS_PROFILE=rigacap. Deploys ~4min via push to main.
- Worker diag: {"parquet_query":{"sql":...}} (DuckDB all_data ≤200 rows), {"parquet_diagnose":true}, run_migration {"sql":[...]} (Postgres), maximizer_preview, {"daily_emails":{"target_emails":[...],"force_tier":...}}.
- Single source=today's dashboard; email GENERATES NOTHING; NEVER truncate lists; each tier own read. Brand claret/paper; NEVER navy/gold/olive; no DWAP/tape/PITFWU to CUSTOMERS (admin/internal OK).
- FORKS have been unreliable here (rate-limits, one died mid-run). Prefer incremental/direct for sensitive prod-data ops.
