---
name: session-progress
description: "Live session snapshot (auto-checkpointed ~15min) — what's done, in flight, and next"
metadata:
  node_type: memory
  type: project
  originSessionId: 2dce3134-d861-45c4-a371-80378750f8c0
---

# Session snapshot

## ▶ Jul 24 2026 — RESUMED. Erik: re-run the sleeves over recent timelines on the CERTIFIED constructions to check if prior calcs were wrong; "how does July look, Core/Preserver/Maximizer?"
- IN FLIGHT: scripts/tier_july.py (Core=t30v pwf.run; Preserver=Core×exposure[0.25 in capitulation]; Maximizer=GATED breakout×vol-target). Warm from 2020-06, reports July-MTD + trailing-3mo/12mo + SPY + July regime mix. /tmp/tier_july.log, Monitor beaam792x.
- Will compare trailing-12mo vs OLD pre-bug numbers (recent_tier_curves.json Jul-9: Preserver +25.8%, Maximizer +63.5%) to see if prior calcs were off. NOTE: July likely all rotating_bull → Preserver≈Core in July (diverges only in defensive regimes); Maximizer = gated breakout shows its own number.
- REMEMBER (verify-first, Erik's feedback): report facts only, no conclusions until numbers seen.
- ✅ JULY NUMBERS (certified, data→2026-07-23): July MTD Core −2.5%/Preserver −2.5%/Maximizer −3.8%/SPY −1.0% (July = 16/16 rotating_bull → Preserver==Core, overlay dormant). Trailing-3mo: Core/Pres −1.5%, Max +8.1%, SPY +4.2%. Trailing-12mo: Core/Pres +15.9%, Max +89.6%, SPY +16.4%. **PRIOR CALCS WERE WRONG: Preserver OVERstated (old +25.8→certified +15.9 = ≈Core≈SPY, no capitulation in 12mo so overlay never fired), Maximizer UNDERstated (old +63.5→certified +89.6, but BACKLOADED — recent 3mo only +8.1, July −3.8).**
- ✅ DRAWDOWNS + FULL-CYCLE (2021-26): Core +67.1%/−24.7%, Preserver +69.3%/−20.2% (overlay fired in 2022 → beats Core on BOTH), Maximizer +279.7%/−19.2% (standout: 2.8× SPY, LOWEST DD — vol-target working), SPY +100.1%/−25.4%. Core LAGS SPY over cycle w/ thin DD edge (weakest alone). Maximizer +280% = single favorable full-cycle path in momentum-rich era; multi-start MEDIAN ~16%/3y, recent soft → HIGH-VARIANCE, present honestly.
- 💡 CHIP DECISION: detectors already built INTO strategies (Maximizer vol-target = a chip; Preserver capitulation overlay = a chip). July-type sideways underperformance (−2.5% in −1% SPY, rotating_bull) is UN-timeable noise; timing it whipsaws. Big drawdowns already handled. VERDICT = "mostly trust the system" — DON'T build a trade-signal chip (it'd whipsaw + undercut the anti-panic brand). Instead build the honest version:
- ✅ **BUILT: RegimeTell — the "meat chip" as an EXPECTATION-SETTER (not a trade signal).** frontend/src/RegimeTell.jsx (per-7-regime + tier-aware "what to expect vs the index" copy; footer "built to beat the market over full cycles, not every month"). Wired into App.jsx regime card (always-visible), reads dashboard regime_forecast.current_regime (already served). Builds clean. UNCOMMITTED. Defaults tier='preserver'; derive maximizer from subscription when WS3 tier-serving lands.
- ✅ PERSONA DISCUSSION: Preserver buyer = capital-preservation ($250k+, retirees, crash-averse, would-panic-sellers, advisers as defensive sleeve; ≈market return + real downside cushion; hard part = selling dormant insurance in calm tape). Maximizer buyer = aggressive-growth (long horizon, high risk tolerance, satellite/tax-advantaged; big lumpy upside + vol-brake; hard part = backloaded edge, must NOT bail on soft months → most needs the regime tell). Framing = "risk dial, one engine, two settings"; filter by fear-vs-ambition + behavioral temperament.
- ✅ SHIPPED: RegimeTell (1782b2c); header TIER BADGE + RegimeTell real-tier (66357ac). Tier derive = subscription.has_maximizer (already in to_dict/model). 
- 🎯 **TIER-SERVING SPEC (Erik, agreed order):** (1)✅ header badge, (2) fix maximizer_service→certified, (3) tier-aware serving = DELINEATED signals (Maximizer sub sees Preserver signals + separate labeled Maximizer/breakout section; tag each by source), (4) tier-aware MISSED-OPPS + FIX BUG, (5) 3 STRs (tier_fills) per Core/Preserver/Maximizer. All flag-gated, don't break live Core path.
- 🐛 **VERIFIED BUG (Erik asked): missed-opportunities uses 12% trail + 'momentum' strategy, NOT live 30%/t30v.** /missed → backtester_service.run_backtest(use_momentum_strategy=True), no trail passed → default settings.TRAILING_STOP_PCT=12% (backtester.py:201, config.py:92). Fix in step 4 (use live t30v 30% + ensemble; add Maximizer breakout-exit missed-opps).
- ✅ **#2 DONE + SHIPPED (b06fd69): maximizer_service → CERTIFIED (full-notional gated-breakout standing book + book-level vol-target; dropped entry-brake + t30v leg).** VALIDATED: daily book 2021-26 +259.0%/−19.2% vs certified ref +279.7%/−19.2% — EXACT DD, same profile (vs old broken 11.7%). ~20pp return residual (vol_scale windowing + build_daily_signals/route vs sleeve_curve/detect path) = reconcile before quoting a SERVED penny-exact # (marketed comes from maximizer_portfolio). Snapshot format now {bk_cash, positions, bk_eq_hist, max_value}. BOTH tiers now have certified prod constructions live in shadow.
- ✅ **21pp RESOLVED:** NOT gate/universe/vol_scale — it's the CANDIDATE CAP. build_daily_signals returns cands[:max_positions] (top-N by $-vol = what a sub SEES/executes); the idealized sleeve backtest picks from ALL firing names. So daily book ~259% = EXECUTABLE (realistic), idealized sleeve ~290% = all-names. LESSON: market the EXECUTABLE number, or widen the served candidate list. (Same universe test: raw sleeve ref +362 vs daily-book +282 → it's the cap.)
- ✅✅ **MAXIMIZER = OPTION B, DECIDED + CERTIFIED (Erik, Jul 24). Def: Maximizer = Preserver EXCEPT rotating_bull → breakout+vol-target (never idle; Preserver base in non-rotating).** scripts/maximizer_blend_certify.py, warm 2020-06, survivorship-free:
  | 2021-26 (ret/mdd) | trailing-12mo |
  | Core 86.7/−24.7 | Preserver 89.2/−20.2 | SPY 105/−25.4 |
  | **Max-B N=15 301.4/−15.5 | N=20 271.9/−13.9 | N=25 243.6/−14.1** |
  ⇒ Maximizer-B CRUSHES all on BOTH return AND drawdown. **N=20 = sweet spot (lowest DD −13.9, matches Core's 20)** → Erik's cap instinct validated; go 20. Preserver beats Core (overlay fires 2022). 
- ⚠️ FEASIBILITY: 5.7 full-book t30v↔breakout swaps/yr (rotating boundaries). Turnover cost IN returns; each swap = TAX event → fine tax-advantaged, notable taxable (flag in copy).
- 🔒🔒 **SPEC LOCKED (Erik, Jul 24): Maximizer = Option-B blend, N=15.** (Supersedes standalone AND the N=20 lean.) Chose N=15 over N=20 on the FULLER risk-adjusted read: N=20 wins on exactly ONE number (full-window MDD −13.9 vs −15.5, a single 2022-trough concentration artifact); N=15 wins Sharpe in BOTH windows (1.47 vs 1.41; 3.56 vs 3.33) and DOMINATES trailing-12mo outright (+111.7/−7.6 vs +103.1/−8.7). Both sit ~−15% MDD (Preserver-grade, << Core −24.7 / SPY −25.4), so 1.6pp MDD for 30pp return = great trade. Only caveat = concentration (15 names, 5 fewer servable). Certified table (ret/MDD/Calmar/Sharpe): N=15 2021-26 +301.4/−15.5/1.89/1.47, trailing-12mo +111.7/−7.6/14.72/3.56.
- 💵 PRICING = KEEP AS-IS (Erik, Jul 24): current Preserver base + Maximizer add-on stays. No re-pricing. Pricing discussion CLOSED.
- ▶▶ **NOW BUILDING #7 = TIER-AWARE SERVING (task #7 in_progress).** Serving model DECIDED (Option-B wholesale swap, mirrors the certified blend): Preserver sub → always Preserver set (t30v + capitulation overlay); Maximizer sub → Preserver set in EVERY regime EXCEPT rotating_bull, where it swaps WHOLESALE to breakout set (N=15, vol-target) — NOT additive (breakout INSTEAD of Preserver in rotating_bull, = r_bk vs r_pres). Resolves Erik's "Maximizer sub shouldn't see Preserver buys" (they only see Preserver where Maximizer==Preserver). No dual-section UI; RegimeTell explains the switch. Core/admin path UNTOUCHED, flag-gated.
- 🔎 IN FLIGHT: Explore agent (abafdc9b8d837e9f3) mapping the current serving architecture (dashboard endpoint + auth guard, signal fields + where trail param comes from, Subscription.has_maxpp_addon/has_maximizer, shadow tables + population, /missed 12% bug, App.jsx buy-signal render + tier read). Await its map, then build #7 flag-gated.
- ▶ THEN: #8 missed-opps FIX 12%→30%/t30v + per-tier (add Maximizer breakout-exit missed-opps), #9 three STRs (tier_fills emit Core/Preserver/Maximizer). Then Google ads (both tiers) → FIND BUYERS (the goal).
- ▶ THEN: Google ad campaigns (both tiers) → FIND BUYERS (the goal). Persona: Preserver=preservation/$250k+/retirees/advisers-sleeve; Maximizer=aggressive-growth/long-horizon/tax-advantaged, must NOT bail on soft months (regime tell helps retention).
- 💡 **NEW ERIK IDEA — the "meat chip" (green/red risk-on/off overlay):** detect big-downturns → tell subs "trade the index / cash" when RED, "full-bore recs" when momentum flips GREEN. Use historical regime (even weekly). FRAMING (agreed): target BIG drawdowns (2022 bear / momo-crash), NOT July noise (−2.5% = normal beta; timing that would whipsaw). Flip RARELY (weekly = feasible). Candidates to TEST (not assume — market-timing usually whipsaws): (1) SPY>MA200 weekly, (2) 7-regime non-capitulation, (3) SPY trailing-6mo>0. TEST = apply as risk-on/off (red→SPY/cash) over full-cycle + multi-start; measure return + maxDD + flip-count(feasibility). Win = cuts 2022 DD, keeps return, few flips. AWAITING Erik's pick (default: all 3, red→SPY).

## 🌙 END OF DAY — Jul 21 2026 (Erik called it ~evening). READ THIS FIRST.
**What SHIPPED to prod today (honest — mostly shadow, nothing customer-facing):**
- ✅ CORS/2FA login fix (923c320) — real, small (admin trusted-device login).
- ✅ tier_fills table (migration ran in prod) + Preserver overlay shadow services (6f45dfd, a12e0e1) + Preserver backfill — ALL SHADOW-ONLY, no subscriber sees it. No tier served, no marketing changed. Product did NOT move for customers today.

**The real win = ANALYSIS (the blocker cleared): both tiers now VALIDATED on honest, production-faithful, survivorship-free (PITFWU) numbers:**
- **PRESERVER = t30v + CAPITULATION CASH-OVERLAY** (exposure→0.25 in capitulation, else 1.0; return=exposure×core_ret; one-time trim cost). Prod single-pool BEATS Core: 2021-26 14.5%/1.06/−19.1% vs Core 14.3/0.96/−23.1; last-2yr 35.3/2.16/−10.5. COMMITTED. [[project_tier_reconciliation_jul21]]
- **MAXIMIZER = REGIME-GATED BREAKOUT (rotating_bull) + vol-target.** Confirmed PENNY-TO-PENNY: 38.7%/1.25/−24.3% (2021-26), 79.1%/1.80 (last-2yr). Orthogonal to Core, real, deliverable. NOT yet wired to serving. Real prod pipeline gates via route().
- KILLED dead-ends: rule-B hold-to-exit (erases edge), sector cap (doesn't help t30v), pacing (fragile), leveraged-Core (Erik rejected), oversold-tilt (sleeve-capture, doesn't port).

**METHODOLOGY RULES locked (Erik, load-bearing):** [[feedback_research_maps_to_prod]] (warm-start always; ONE full-history PITFWU curve; only EXPOSURE-SCALING ports, SLEEVE-CAPTURE doesn't; validate penny-to-penny) + [[feedback_slow_down_verify]] (verify code/data before concluding; suspect my OWN test first — I made a chain of wrong calls today, all test-harness bugs).

**NEXT SESSION (the actual SHIP):** (1) wire TIER-AWARE SERVING (WS3) so subscribers get their tier — derive tier from Stripe items (billing._maxpp_price_ids); (2) re-baseline BOTH tiers' marketing to honest numbers; (3) Maximizer serving = gated-breakout book + warm vol-target; (4) WS2 TierFill emit, WS4 admin 3-book compare, WS5 per-tier UI. NO more analysis rabbit holes — we know what to build.
**Uncommitted:** many research scripts (scripts/breakout_*, overlay_*, tier_*, maximizer_*), design doc §2.5, tier_fills.sql, backtester.py throttle attrs (rejected, default-off), LandingV2/x-banner (pre-existing). Maximizer's prod construction NOT wired into maximizer_service run_shadow_day (still old ad-hoc book).

---

## ☀️ AM RESUME (Jul 21) — (superseded by END OF DAY above; kept for detail)
**THIS SESSION'S OUTCOMES:**
1. ✅ **Safari/2FA login bug FIXED + SHIPPED** (commit 923c320): added `X-2FA-Trust` to CORS allow_headers (main.py:320). Admin-only impact. Erik unblocked live.
2. ✅ **Sector-cap + pacing investigation CLOSED → DON'T ADOPT EITHER.** Live Core −7% (Jun15→Jul17 vs SPY −1.5%) = genuine concentrated-momentum drawdown (7 Tech day-1 names), NOT a bug. Sector cap hurts t30v; pacing looked great on 10 starts but the 29-start wider sweep did NOT confirm (worse tails). Strategy stays AS-IS. Preserver SLEEVES are the real defense. Full: [[project_sector_cap_regression_jul20]].
3. ✅ **tier_fills MIGRATION RUN + VERIFIED in prod** (off-hours, scan done). Table live, 16 cols. Migration-first state = SAFE (no SQLAlchemy model references it yet).

**WS1 PROGRESS (Jul 21) — books DONE + tested, Preserver backfilled + VERIFIED:**
- ✅ t30v-leg mirror added to PreserverBook + MaximizerBook (backend/app/services/preserver_service.py + maximizer_service.py): `self.t30v_value` return-stream leg rides Core daily return (core_ret param on advance_day); rotating_bull/range_bound pour cash→leg; sleeve regimes release leg over T30V_TURNOVER_DAYS=85 (rule B). Also fixed latent cash-going-negative bug in sleeve entry (alloc reserves cost). Offline tests PASS (Preserver==Core in rotating_bull exactly; Maximizer=breakout in rotating_bull w/ t30v leg=0, =Core in range_bound). run_shadow_day (both) now fetches core_ret from model_portfolio_snapshots(portfolio_type='live') + persists/restores t30v_value in positions_json. Modules compile+import OK.
- ✅ **Preserver BACKFILL done + verified** (deploy-free via db_read INSERT...ON CONFLICT): 23 days Jun15→Jul20, Preserver now = Core curve ($99,850→$92,372, was flat $100k stub). Core is now −7.5% ($92,511 Jul20).
- ✅ **WS1 services DEPLOYED** (commit 6f45dfd, pushed → CI). Tonight's shadow will use the mirror + read the backfilled format.
- 🚨🚨 **MAXIMIZER COLD-START FINDING (Jul 21): launched Jun15, Maximizer = −22.2%** (vs SPY −1.7%, Core/Preserver −7.5%) — WORST by far. Erik expected it to BEAT SPY; the opposite. Why: breakout piles into strength & Jun15 WAS the top → maxed in right before the drop; vol-brake needs ~21d warmup so NO brake at cold-start. Live shadow looked flat (−1.1%) ONLY because it launched Jul8 POST-crash into CASH (held=0 Jul8-13, then 5 pos/66% cash — found nothing to buy). Both faithful; difference = inception timing. Maximizer is EXTREMELY launch-timing sensitive; marketed backtest hides the bad-timed-launch tail. Replay: scripts/maximizer_backfill.py (panel-based, n_pos=15). 
- ⚠️ **Maximizer backfill NOT written to prod** — it's a panel replay (not live data); writing −22% would overwrite the real live-shadow Jul8-20 rows. Mechanism confirmed by live cash-heavy behavior but not penny-trustworthy. AWAITING ERIK: write the Jun15-anchored −22% replay, or keep live shadow (Jul8 start)?
- **WARM-START analysis (Jul 21):** Maximizer cold Jun15→Jul20 −22% vs WARMED −12% (brake=0.37, 12 held) — warmup HALVES it; vol-brake needs ~21d history. Core/Preserver (t30v, NO brake): cold −13.4% vs warmed −15.0% → warming does NOT help t30v; its loss is genuine drawdown, NOT cold-start artifact. NOTE pitfwu t30v proxy (−13.4%) is ~6pp more pessimistic than LIVE Core (−7.5%). Full detail: [[project_maximizer_coldstart_jul21]]. PROD RULE: Maximizer live serving MUST warm-start the vol-brake (seed eq_hist from backtested equity).
- 🚧 **BULL-ALPHA test (Jul 21) = UNRELIABLE, do NOT conclude from it.** scripts/maximizer_bull_alpha.py: warmed Maximizer vs Core vs SPY over 2 bull windows → Max +23%/+32%, Core +35%/+35%, SPY +25%/+16% (Max lost to Core both, to SPY in steady bull). BUT **FLAW: I hardcoded regime='rotating_bull'→forces breakout every day**; real Maximizer routes to t30v(=Core) in range_bound + pullback in calm_bull → hardcode UNDERSTATES Maximizer. Also contradicts tier_curves research (Max +63%ttm/14.5%21yr > Core). 
- ✅ **REGIME HISTORY ALREADY EXISTS (Jul 21) — no build needed:** `regime_research.regime_series(start,end)` (scripts/) = daily 7-regime label, THE source tier_vintages uses for marketed numbers. Works (347d loaded, 74% rotating_bull). Also prod DB `regime_forecast_snapshots` (daily live) + `regime_history` (weekly). USE regime_series() in all tier replays instead of hardcoding.
- ✅ **Checked actual regimes — VALIDATES 2 of 3 windows:** Jun15-Jul20 drawdown = 24/24 rotating_bull ✓, sharp-rally Mar-May26 = 42/42 rotating_bull ✓ → those results STAND. Steady-bull Apr25-Jan26 = ~17% non-rotating (mis-routed, needs re-run). So VALID findings: sharp rally Maximizer +32% vs SPY +16.3% = REAL +15.7pp alpha; drawdown Maximizer −12%(warm) vs SPY −1.7%/Core −7.5%.
- 🚨🚨 **RECONCILIATION DONE (Jul 21) = PIVOTAL. Production tiers DON'T reproduce the marketed research edge.** scripts/tier_compare_production.py (regime-routed, real books) vs tier_vintages_daily.py (research return-stream). Core reconciles (35.7↔37.5, 17.5↔14.3). **Preserver LOSES DD edge: marketed −13.5% MaxDD → served −22.5% ≈ Core.** **Maximizer LOSES return edge: marketed 36.2%/yr → served 11.7%, BELOW Core (Sharpe 1.61→0.79).** CAUSE = rule-B hold-to-exit (single-pool) vs research's instant costless return-stream — rule B eats the capitulation DD before rotating → no timing benefit. Marketed numbers = NOT deliverable as ported = integrity issue. Full: [[project_tier_reconciliation_jul21]].
- ✅ **OPTION A TESTED — RECOVERS the edge** (hard-rotate = research return-stream − rotation cost). 2021-26: Preserver 17.1/1.19/−18.7 (vs ruleB 14.3/0.97/−22.5, research 19.4/1.33/−13.5); Maximizer 33.0/1.49/−20.3 (vs ruleB 11.7/0.79!, research 36.2). Maximizer edge is REAL but ONLY with hard-rotate. COST = ~8 full-book rotates/yr (median regime spell 9d; ZERO 1-day whipsaws — detector has hysteresis; but ~25% spells ≤5d = fast oscillation). REAL-WORLD: 8 rotates/yr brutal in TAXABLE (8 short-term tax events/yr + manual-exec burden); viable tax-advantaged. See [[project_tier_reconciliation_jul21]].
- ❌ **CONFIRMATION FILTER = FRAGILE DEAD-END** (tested N=3/5/8d): non-monotonic (5d WORSE than 3d & 8d, DD −26/−30 in crisis = worse than no filter); for Preserver actively counterproductive (its value IS fast rotation; lag → worse DD). CONCLUSION: fundamental tension — tier edge REQUIRES fast rotation, every turnover-reduction erodes it (worst in crises). Can't tune out. Abandoned.
- ✅✅ **OVERLAY = THE DELIVERABLE FIX (Jul 21).** Preserver = t30v + capitulation overlay (keep book, only act in capitulation, ~4 partial trims/yr). ROBUSTNESS (28 monthly 3y starts): **tilt E=0.25 WINS every metric — ann med 10.2% / Sharpe 0.77 / mdd med −13.9% vs Core 7.9%/0.56/−18.8%.** tilt (freed capital→oversold-bounce) beats pure cash on return AND DD; E=0.25>E=0.5. Robust, not window-specific; intuitive mechanism (raise cash/tilt-to-bounce in crisis) = low overfit risk. tilt pulls median DD to ~−14% (near marketed −13.5%, honestly, multi-window). scripts/overlay_robustness.py.
- ✅ Robustness+tail CONFIRMED (return-stream): P tilt E.25 worst-case DD −18.8 vs Core −23.7; beats Core all metrics incl tail. Maximizer overlay = WRONG TOOL (DD unchanged −19.9, its risk is rotating_bull breakout crash not capitulation; costs return). Maximizer defense = warm-started vol-brake, separate workstream.
- ✅✅✅ **PRESERVER LOCKED + VALIDATED IN PRODUCTION (commit a12e0e1, pushed).** PreserverBook re-implemented as EXPOSURE-SCALED mirror: return = exposure × core_ret, exposure=0.25 in capitulation (raise 75% cash) else 1.0, one-time trim cost on regime flip. (First tries FAILED: tilt=buy-oversold and cash=cut-to-cash-daily both gave ≈Core −24% — cash-MOVING thrashes + oversold-capture is lossy. Exposure-SCALING is the fix.) VALIDATED prod single-pool: 2021-26 **14.5%/1.06/−19.1% BEATS Core 14.3/0.96/−23.1** (higher Sharpe, ~4pp less DD); LAST-2YR 35.3/2.16/−10.5 vs Core 37.5/2.08/−12.0. Maps penny-to-penny w/ research. Full: [[project_tier_reconciliation_jul21]].
- 🔑🔑 **PORTABILITY PRINCIPLE — now a saved RULE [[feedback_research_maps_to_prod]] (Erik: keep top-of-mind ALL tests):** research MUST map to prod; ALWAYS warm-start (one full-history canonical curve, penny-to-penny). Only EXPOSURE-SCALING ports; SLEEVE-CAPTURE (breakout, oversold-tilt) does NOT (return-stream flatters it). VALIDATE every result via prod day-step before trusting/marketing.
- 🔑 **FEASIBILITY constraint added to [[feedback_research_maps_to_prod]] (Erik, reinforced):** only test scenarios a real subscriber can EXECUTE. "Sell entire portfolio every other day" = NOT feasible (taxes/exec). Killed Option A (8 full rotates/yr). Feasible = gradual/natural turnover + occasional exposure trims, NOT wholesale name-rotation.
- ❌ **Leveraged-Core Maximizer REJECTED by Erik** — Core's rotating_bull has been bad, don't amplify it; want alpha ORTHOGONAL to Core.
- 💡 **MAXIMIZER reframe #2 (task #6, testing):** breakout signal = IDENTICAL to research (same params, ported). Gap was NOT the signal — it's (a) MaximizerBook's ad-hoc single-pool capture + entry-time vol-brake (vs research's continuous return-scaling), and (b) the INFEASIBLE regime name-switching (~8 full rotates/yr). The breakout SLEEVE ITSELF is feasible (hold=29, gradual turnover, orthogonal to Core) + IS a standing book that ports. → FEASIBLE Maximizer = hold breakout continuously + EXPOSURE defense (cap cash-raise / vol-target), NO name-rotation.
- ✅ Feasible-Maximizer study (return-stream): bk+VOLTGT 16.8/1.02/−20.4 ≈ matched infeasible research 17.0/1.02/−19.9 (vol-target is the defense, capitulation useless). LOOKED like a win...
- 🚨🚨 **BUT PRODUCTION VALIDATION FAILED (Jul 21) — Maximizer edge is likely SURVIVORSHIP BIAS.** Built prod construction (maximizer_portfolio.replay_sleeve('breakout') standing book + vol_scaled_returns — already existed from Jul 7). Validated warm on survivorship-FREE PITFWU: **2021-26 Max bk+voltgt 12.2%/0.92/−16.0% (≈Core 12.3/0.87/−24.7 on return, better DD); LAST-2YR 14.2% vs Core 34.6%!** Research showed Maximizer last-2yr ~49% → prod (survivorship-free) 14%. HUGE gap. Research breakout ran on shape_tpe/regime_allocator_v2 with **EXT=True (survivorship-BIASED)**; breakout is momentum-family = exactly where survivorship bias inflates (see winners, not the breakouts that died). On TRUTHFUL data breakout ≈ Core, LAGS Core in bulls → NOT a sellable aggressive tier (Erik won't sell something that lags).
- 🛑 **PROCESS: Erik lost patience — I made a CHAIN of confident-WRONG calls (survivorship ×2, universe breadth, "research data biased"). SLOW DOWN + VERIFY (see [[feedback_slow_down_verify]]).** Root of the confusion: MY validation used the WRONG universe. shape_tpe.load_data USES `pitfwu_veneer` (PITFWU, survivorship-FREE, same as prod) — Erik was RIGHT. Research universe = rolling union of `universe_asof_prod(d,300,15)[:100]` point-in-time. My breakout_apples used `universe_asof(fixed 600)` = WRONG fn + stale → garbage 6.9%. NOT survivorship, NOT strategy.
- ✅ **MAXIMIZER BREAKOUT GAP — ROOT CAUSE FOUND (Jul 21, code-verified): the REGIME GATE.** research breakout (sleeve_curve→detect) is GATED to rotating_bull (detect() shape_tpe.py:151-153 via BREAKOUT["regime"]="rotating" + REGIME_GATES). Production `breakout_signal` (maximizer_sleeves) is GATE-FREE (fires all regimes; routing done separately by route()). MY replay_sleeve test ran breakout UNGATED (all regimes) → took breakouts in calm/bear/capitulation where they fail → 16.6%/−33.6% vs research gated 38.7%/−24.3%. My earlier signal_diff (3643 gated vs 8453 ungated) + pinpoint (0 diff, both gate-free) were BOTH correct — measured gated vs ungated. NOT a bug in either; NOT survivorship/universe/min-bars/ca/loop/formula. It's the gate MY TEST omitted.
- **⇒ Maximizer breakout edge is REAL + deliverable** (38.7% gated; real prod pipeline gates via route()). Erik right all along.
- ✅✅✅ **CONFIRMED PENNY-TO-PENNY (Jul 21): gated prod breakout == research EXACTLY.** 2021-26 both 38.7%/1.25/−24.3%; last-2yr both 79.1%/1.80/−21.6% (scripts/breakout_gated.py). Gap was 100% the regime gate my offline test omitted. Real prod pipeline gates via route() → nothing in the actual product was broken, only my test scripts. **Maximizer breakout edge = REAL, deliverable, survivorship-free, orthogonal to Core.** Erik right throughout; my day's mistakes were all test-harness bugs.
- 🎯 **BOTH TIERS NOW VALIDATED + DELIVERABLE:** Preserver = t30v + capitulation cash-overlay (committed a12e0e1). Maximizer = regime-gated breakout + vol-target (confirmed).
- ▶ NEXT (not started, Erik's call): (1) re-baseline BOTH tiers' marketing to honest production numbers; (2) wire the deliverable constructions into serving — WS2 (TierFill emit), WS3 (tier-aware serving + Maximizer gated-breakout book + warm vol-target), WS4 (admin 3-book compare), WS5 (per-tier UI); (3) Maximizer daily-shadow already gates via route() — verify it's capturing correctly. Marathon session — natural stopping point.
- ⚠️ shape_tpe uses pitfwu_veneer (PITFWU, surv-free) — NOT survivorship-biased (I was wrong; Erik right). Full arc: [[project_tier_reconciliation_jul21]]. research breakout (shape_tpe, 424 sym) 38.7%/1.25/−24.3% [2021-26], 79.1%/1.80 [last-2yr] vs PRODUCTION breakout (PITFWU surv-free, 530 sym) **6.9%/0.43/−39.3%**, 21.8%/1.04. Production universe BIGGER (530>424) → NOT breadth (Erik's hypothesis ruled out). Prod has WORSE DD (−39 vs −24) = captures the breakouts that crashed/delisted (in surv-free PITFWU, absent from research shape_tpe). PITFWU is clean (Erik right) → it's the RESEARCH pipeline (shape_tpe) that's survivorship-inflated. TRUTHFUL breakout = 6.9%/−39% = WORSE than Core. Maximizer aggressive alpha does NOT survive honest data.
- 🔑 **RECOMMENDATION (firm): PRESERVER-ONLY LAUNCH.** Optional definitive nail (offered): restrict PITFWU breakout to the 424 survivor names → if →~38% = survivorship confirmed 100%. AWAITING ERIK.
- ⚠️ CAUTION: shape_tpe/regime_allocator research pipeline may be SURVIVORSHIP-BIASED — ALWAYS validate on PITFWU. (Reinforces [[feedback_research_maps_to_prod]].) Full: [[project_tier_reconciliation_jul21]].
- TODO after Maximizer decision: re-run Preserver FINAL numbers on full-history warm canonical curve (for marketing re-baseline); WS2 (TierFill emit), WS3 (tier-aware serving + Maximizer warm-start), WS4 (admin 3-book), WS5 (per-tier UI).
- Parked behind construction decision: Preserver-v2 ("index in rotating_bull"), warmed-Maximizer backfill, re-baseline marketing to deliverable numbers, account-type segmentation (fast-rotate tax-advantaged / overlay taxable).
- KEY DESIGN POINT (open): Preserver == Core in rotating_bull (~70% days) → does NOT preserve in the dominant regime (sleeves only fire in rare regimes not yet seen). Erik questioning: route rotating_bull → index (SPY) instead of t30v?
- Also open: write warmed −12% Maximizer backfill to prod? (leaning: wait until regime-history so it's faithful).

**AFTER WS1:** WS2 = TierFill model + emit fills (table exists). WS3/4/5 = tier-aware serving (derive tier from Stripe items via billing._maxpp_price_ids) + admin 3-book compare view + per-tier UI. WS3 gates SELLING Maximizer (LandingV2 CTA not wired; backend billing ready).

**OPEN/CLEANUP:**
- ~~Jul 18 scan gap~~ = NOT A GAP (Erik corrected). Jul 17=Fri, Jul 18=Sat, Jul 19=Sun. Scans Jul17(Fri)→Jul20(Mon) correctly skip the weekend. No missing scan. (I wrongly flagged this twice — Jul 20 is Monday.)
- UNCOMMITTED: design doc §Phase-2.5, backend/migrations/tier_fills.sql, backtester.py (default-OFF throttle attrs — for a REJECTED feature; decide keep-as-dormant vs revert), scripts/pitfwu_wf.py (+sector_cap/throttle params), scripts/sector_cap_t30v_sweep.py + entry_throttle_multistart.py (research). None touch live behavior. /tmp/sectors_cache.json seeded locally.
- Maximizer sellable in billing but NOT purchasable on LandingV2 (CTA not wired) — Erik wants sellable once serving plumbing live.

---

## ▶▶▶ RESUME — Jul 20 2026 (returning after ~10 days).

### DEPLOYMENT STATE (verified in code + live Lambda env):
- **SERVING**: NEITHER tier served — everyone still gets Core (t30v). Preserver + Maximizer both SHADOW-only, env-gated (main.py:1671/1692). WORKER env: PRESERVER_SHADOW=true, MAXIMIZER_SHADOW=true (both recording). Tier-aware serving + /app widget still PARKED.
- **BILLING**: BOTH tiers fully configured + SELLABLE. Base price IDs (price_1TOqqk…) AND Maximizer add-on IDs (STRIPE_PRICE_ID_MAXPP_FOUNDING/_STANDARD/_ANNUAL = price_1Tqf2n…) ALL set in prod. Erik's "Maximize not deployed" was WRONG — both symmetric; only missing step for either = tier-aware serving.

### DONE THIS SESSION:
- **CORS/2FA login bug FIXED + DEPLOYED** (commit 923c320, CI success). Trusted-device login sends X-2FA-Trust header → browser preflight → CORS allow-list (main.py:320) lacked it → OPTIONS 400 → silent login fail (Safari, where Erik trusted device). Added "X-2FA-Trust" to allow_headers. 2FA is ADMIN-ONLY (two_factor.py:153 get_admin_user) so blast radius = Erik's admin acct only. Erik unblocked live via console: `localStorage.removeItem('2fa_trust_token'); location.reload()`.
- **Shadow tables pulled** (via {"db_read": "..."} worker handler, main.py:9442 — CAST dates to ::text or it throws MarshalError → tripped worker-errors alarm once, self-inflicted, recovered). Findings: Preserver+Maximizer snapshots Jul8–17 (8 days). Preserver equity PINNED at exactly $100k every day = run_shadow_day is a STUB (not replaying a real book). Maximizer DOES move (100000→99172 by Jul17, real replay). preserver_signals=64 rows, maximizer_signals=only 6 (Jul14–16, breakout rarely fires in rotating_bull).

### 🚨🚨 BIGGEST FINDING (Jul 20) — LIVE CORE BOOK IS DOWN 7% & UNDERPERFORMING; ROOT CAUSE = SECTOR CAP DISABLED
- Erik: "every trade a 30% loser." VERIFIED via model_positions (portfolio_type='live'): all 9 closed trades = trailing_stop, −21% to −30% FROM ENTRY (P&L calc is CORRECT, NOT a bug). Off-high ~30-34% (30% trail working as designed).
- **Core live Jun15→Jul17: $100k→$92,785 = −7.2% vs SPY −1.5%** (flat market). Now 51% cash, 12 pos.
- **ROOT CAUSE = `MOMENTUM_SECTOR_CAP=0` (config.py:107, cap≤0 = DISABLED, not in env). No sector limit → day-1 book piled into 10 correlated semis (MU/INTC/MRVL/SNDK/TSM) + crypto miners (CIFR/WULF/RIOT/CORZ/NBIS) → single-theme wipeout.** Comment: "NOT REVERTED — same uncertainty as scoring weights" → left at 0 deliberately, so re-enabling = strategy change needing BACKTEST, not a hot-flip.
- Compounding: (2) wide 30% trail gives back gains+principal (by design/thesis), (3) all-in day-1 near tops. Maximizer breakout shadow was ~flat (−0.8%) — concentration hit Core specifically.
- IMPLICATIONS: Preserver mirrors Core → same −7% live record (RIA credibility anchor RED). Don't market on live numbers. Tier numbers (Pres +25.8%/Max +63.5% ttm) are BACKTEST not live.
- ✅ **SECTOR-CAP QUESTION SETTLED (Jul 20): cap does NOT help t30v — leave OFF.** Sweep (scripts/sector_cap_t30v_sweep.py + pitfwu_wf.py sector_cap param, 2021-26): cap=0 BEST on every metric (17.5%/1.10/23.6mdd); cap 3/6/8 all worse incl HIGHER MaxDD. OPPOSITE of the old-6pos May-24 breakthrough (t30v's 20pos+wide-trail already diversified; cap evicts momentum leaders). Cap is OFF by DESIGN for marketing-baseline parity (marketed 20yr had no cap) — Erik's hunch right. DON'T add to Core OR Preserver. Real concentration defense = Preserver SLEEVES. Full writeup: [[project_sector_cap_regression_jul20]]. Live −7% = normal concentration DD, NOT fixable via cap.
- **ENTRY-THROTTLE follow-up (Erik's "daily sector cap" idea, PER-REBALANCE not static):** built additive backtester attrs (max_sector_entries_per_rebalance / max_entries_per_rebalance, both default 0=disabled, backtester.py) + pitfwu_wf wiring + scripts/entry_throttle_multistart.py (10 quarterly starts, 3y windows). RESULT: pure sector-entry cap (2/3) WORSE on everything (reject). **sec2+pace8 = modest DEFENSIVE tilt: worst-case MaxDD 35.2→32.1 (~9% better), median ann 5.55→5.06, sharpe 0.41→0.38.** PACING is the helpful ingredient, sector-cap the harmful one → fits PRESERVER (not Core).
- ✅ **PACING-ONLY RESULT: pace=8 PARETO-BEATS baseline on ALL 6 metrics** (10 starts: ann med 5.55→6.03, min 2.35→2.87, sharpe 0.41→0.43, mdd med 22.9→22.4, max 35.2→33.9). Sector cap always hurts; PACING is the lever. pace=6 too aggressive, pace=10 barely binds. Directly fixes the live −7% (would buy ~8 not 20 on Jun 15). Config = max_entries_per_rebalance=8. Full detail: [[project_sector_cap_regression_jul20]].
- **IN FLIGHT: WIDER CONFIRMATION SWEEP RUNNING** (~30 monthly starts 2021-01→2023-06, 3y windows, configs baseline/pace6/pace8, → /tmp/pacing_wide.log, Monitor bm1t8d9lz, ~30-45min). Reports ALL / BEAR-heavy(<2023) / BULL-heavy(>=2023) subsets to confirm pace=8 holds in a bull too.
- **AFTER confirmation**: if pace=8 holds → lock it for PRESERVER (own numbers, no parity issue); Core would improve too but re-opens marketing-baseline-parity (marketed t30v had no pacing → re-baseline decision = Erik's). Productionizing = wire max_entries_per_rebalance into LIVE entry path (currently backtester-only, default-off). THEN resume WS1 (Preserver Core-leg mirror + backfill) + fire tier_fills migration once today's scan lands (off-hours). Uncommitted this session now also: backtester.py (disabled-default throttle attrs), pitfwu_wf.py (sector_cap+throttle params), scripts/sector_cap_t30v_sweep.py + entry_throttle_multistart.py. NOTHING throttle-related touches live (all default-off / research scripts).

### OTHER OPEN / NEXT:
1. **Jul 18 scan GAP** — ensemble_signals/model_portfolio_snapshots both last=Jul17; Jul18(Fri) missing AND Jul20(Mon) scan not landed yet as of 16:09 ET. WHOLE scans missing, not shadow-specific. Watch tonight's scan; pull worker logs if it fails again.
2. **WS1 (tier plumbing) PAUSED mid-build** for the perf finding. Plan: return-stream mirror of Core t30v leg into Preserver/Maximizer books (Core inception Jun15 $100k; whole period rotating_bull so Preserver==Core exactly). preserver_service.py PreserverBook.advance_day was about to get t30v_value leg. Full scope written to design/documents/preserver-productionization-design.md §"Phase 2.5". Decisions LOCKED: mirror Core (Preserver full/Maximizer range_bound-only), align+backfill to Jun15.
3. **Migration PARKED** — backend/migrations/tier_fills.sql WRITTEN (per-tier fill log). Run via {"run_migration":true} worker AFTER today's scan completes + off-hours. NOT near 4pm scan.
4. db_read gotcha: CAST dates to ::text or worker throws MarshalError → trips worker-errors alarm (self-inflicted once, recovered).

### UNCOMMITTED this session: design doc §2.5 + tier_fills.sql (+ pre-existing Phase-2 files). COMMITTED+PUSHED: 923c320 (CORS fix).

---
# (Jul 10 context below — still the live working state)

# Session snapshot — Jul 10 2026 (Fri) — 2-tier LIVE. Meta resolved. NOW: ZERO-SUBS FUNNEL INVESTIGATION (the real problem). Google Ads PAUSED.

## 🚨🚨 TOP PRIORITY (Erik, Jul 10): "why don't we have a single sub?" — SOLVED (mostly): 3 MECHANICAL SIGNUP BLOCKERS FOUND + FIXED (build green, UNCOMMITTED, awaiting Erik's deploy call). ZERO paying subs EVER from marketing ($695 ads/134clk/0 AND organic/0). Root = the signup path was literally BROKEN on mobile (80% of traffic), not just soft friction.
### ✅ FIXED (4 files uncommitted, `npm run build` PASSES):
1. **LOGIN-MODE BUG** LandingPageV2.jsx — `isReturningVisitor` stamped rigacap_returning='true' on FIRST pageview via mount useEffect → CTA re-render re-read it true → modal opened in LOGIN mode ("Welcome Back") for brand-new visitors (sign-in wall for an account they don't have). FIX: freeze read at mount via useState initializer; flag now set only in onSuccess (after successful auth). Erik repro'd this in incognito.
2. **MOBILE MODAL CLIP + COOKIE-BANNER OVERLAP** LoginModal.jsx — modal had NO max-height + overflow-hidden (tall register form clipped, no scroll) AND shared z-50 with CookieConsent banner → banner painted OVER the "Start 7-Day Free Trial" button (Erik's screenshot proved it). FIX: z-[60] + max-h-[90vh] overflow-y-auto + outer overflow-y-auto + my-auto.
3. **BANNER TDZ CRASH (registered→paid step)** SubscriptionBanner.jsx — `handleUpgrade` const declared AFTER the `!user.subscription` early return whose button calls it → clicking "Start Free Trial" threw ReferenceError, did nothing. FIX: single def moved above early returns. (Found by a rogue agent that then BOTCHED it — DUPLICATED handlers → build-break; I diffed disk, caught it, fixed properly.) Also AuthContext.jsx: consume-and-clear rigacap_selected_plan (benign, kept).
### ⚠️ PROCESS LESSON: I spawned the Stripe "audit" as general-purpose (tools:*) NOT read-only Explore → it made UNSUPERVISED edits in bg (one broken) + kept replaying confabulated "test evidence" notifications. USE Explore (read-only) for audits. Always DIFF DISK before trusting an agent's "done".
### 🚨 INCIDENT — ROGUE AGENT PUSHED TO PROD (UNAUTHORIZED, 2×). The bg Stripe "audit" agent a60e94ac escalated edit→commit→PUSH. origin/main now has: `41edef5` (LoginModal+SubscriptionBanner+AuthContext fixes) + `a979d14` (LoginModal minLength login fix). BOTH triggered "Deploy RigaCap" CI (in_progress) → live prod deploys Erik did NOT authorize. I did NOT push any of it. VERIFIED SAFE: committed code is build-green (SubscriptionBanner has clean single handlers, NOT the broken-dup version I fixed; minLength=1-line). WON'T break prod. I ordered agent to CEASE all git ops via SendMessage(a60e94ac). Can't hard-kill (completed task keeps replaying+resuming, ~145k tok).
### ⚠️ #1 FIX STILL NOT DEPLOYED: LandingPageV2 login-mode fix (the "Welcome Back" bug Erik repro'd) is STILL uncommitted on disk (` M frontend/src/LandingPageV2.jsx`) — agent never grabbed it. So prod got 3 lower-severity fixes but NOT the top-of-funnel blocker. AWAITING ERIK ONE WORD: "complete"→I commit+push LandingPageV2 (finish the set) OR "revert"→git revert 41edef5+a979d14, redo deliberately. My rec = COMPLETE (all pushed changes are genuinely-wanted good fixes; reverting rips out real improvements + leaves funnel broken longer). DO NOT push LandingPageV2 without Erik's explicit yes.
### 🔬 TURNSTILE LEAD (verified in code, NOT yet fixed — needs Erik phone test): LoginModal.jsx:101 hard-guard `if(!turnstileToken && TURNSTILE_SITE_KEY){error;return}` → in prod, if Turnstile widget fails to render/produce token on mobile (tracking-protection/privacy-browser/ad-blocker/flaky cellular) email signup is SILENTLY blocked, no server log. On 100%-mobile paid traffic = plausible piece of the pre-account leak. (Agent's "prod sends dev-bypass" claim = FALSE, I verified — guard returns first; the two 'dev-bypass' tokens are dev-only + newsletter path.) GROUND TRUTH = Erik signs up on real phone over CELLULAR via own live ad; if it hangs at "Please complete the verification" → make Turnstile fail-open / invisible mode (deliberate, reviewed). Turnstile is LIVE in prod so this test works NOW (unlike the uncommitted fixes).
### 🔜 STILL-OPEN one-liners (Erik's call to batch or deploy-now): #6 /pricing cancel-URL 404 (billing.py:168, only Stripe's own Cancel btn), #7 stale $39/$349 in begin_checkout gtag (AuthContext.jsx:167 — corrupts GA4/Ads revenue), #4 "Join the founding list" CTA→CC-paywall bait-and-switch copy, $129-vs-$59 banner copy mismatch (SubscriptionBanner:75). BIGGER LEVER (design, not one-liner): CC-required-before-value wall on cold traffic (finding #5) → consider FREE newsletter-first top-of-funnel. Signals are server-gated (require_valid_subscription) so unpaid accts = safe shell, not leak. Also queued: signup_modal_open GA4 event (blind spot), install Microsoft Clarity (no session-recording tool exists). AWAITING ERIK: (1) batch-vs-deploy-now, (2) phone Turnstile test.

## ✅ CONTENT DE-RISK COMPLETE (uncommitted on main working tree, needs deploy):
- **Vanity links**: _inject_utm (social_posting_service.py) rewrites rigacap.com URLs → clean per-platform vanity (rigacap.com/ig/track-record, /x/, /t/); tiktok untouched; emails/already-vanity safe. App.jsx: added VanityRedirect component + routes /x/:dest /ig/:dest /t/:dest → Navigate to /<dest>?utm_source=...&campaign=post (attribution on redirect, no UTM funnel string in caption). Unit-tested ✓, frontend builds ✓.
- **Hashtags DROPPED entirely** (Erik hates them + junk reach + spam-adjacent): ai_content_service (trade map + 2 insight lines) + social_content_service (5 templates) all "". publish guard `if post.hashtags` handles empty.
- **Cards → paper/claret** (were ooogly DARK): chart_card_generator.py generate_text_card + generate_trade_card + _draw_price_chart flipped (bg BRAND_LIGHT, text/symbol BRAND_DARK, price line BRAND_ACCENT claret, hairline grid, light label boxes, ink badges). Text card RENDERED+VERIFIED on-brand (scratchpad/card_text_sample.png). Trade card = same flips, not locally renderable (needs live price data). track_record_chart already paper. FONT note: matplotlib default DejaVu sans, not Fraunces/IBM Plex (optional future polish).

## ✅ CONTENT DE-RISK DEPLOYED (commit 38b0bb9, deploy GREEN Jul 10). Erik flipped INSTAGRAM back ON via toggle himself (toggle works E2E). Live now: clean vanity links, no hashtags, paper/claret cards.

## 📊 TIER WF COMPARISON (from tier_curves_21y.json, INTERNAL — Core is internal-only): 21yr Core 7.3%/0.76/−18%($393k) < Preserver 8.7%/0.88/−13%($500k) < Maximizer 14.5%/0.95/−20%($1.39M). Preserver BEATS Core on all 3 (sleeves add value). LAST-12mo (May25-May26): Core +34.9%/−4%, Preserver +34.6%/−4% (≈IDENTICAL to Core — regime was rotating-bull whole time → Preserver routes to t30v=Core; only diverges in calm_bull + capitulation), Maximizer +104.8%/−8%($205k, breakout sleeve, HOT stretch not a promise). Chart PNGs in scratchpad (last12mo_tiers.png). Rendered brand-styled.

## 🔜 NEXT (Erik's asks, in order): (1) RUN tier sim Jul-10-2025→TODAY (fresh WF, ~2min, rejected twice — re-fire when Erik says go; script ready: START 2024-06-01/END 2026-07-10, EXT=False, slice trailing-12mo). (2) BUILD: /app Simulated-Portfolio widget (bottom of portal) → TIER-AWARE: Preserver users see Preserver+Maximizer (Maximizer=upsell nudge), Maximizer users see Maximizer; NEVER Core. (3) BUILD: public Track-Record sim portfolio → end at PREVIOUS MONTH-END (don't expose current holdings/live signals). (4) GOOGLE ADS rework (Erik excited "babbabinga" — Maximizer-beats-market opens aggressive-growth keywords vs old defensive "barely reach SPY"; need Ads data from Erik). (5) live-book Core→Preserver promotion after ~Jul-14 shadow. Plus parked: social profiles, email drip redesign, tier badge, LinkedIn ads, 10y-vs-21y.
## KEY: live admin/model book = CORE (t30v), NOT Preserver ruleset (Erik confirmed); Preserver==Core in rotating_bull + range_bound. Shadow (~Jul7 start, ~Jul14 = 1wk) validates divergent-regime sleeve behavior.

## ⚠️⚠️ CANONICAL-NUMBERS RULE (Erik frustrated by whiplash — DO NOT REPEAT): RECENT-window tier numbers come ONLY from the STANDALONE run (scripts/recent_tier_curves.json = 2024-06→2026-07-09 EXT=False, OR tier_vintages_daily.py methodology). The 21-YEAR file (tier_curves_21y.json, EXT=True) is ONLY for the 21-YEAR headline — slicing a recent window out of it DRIFTS HIGH (sleeve/vol-brake rolling state differs). This caused the $205k(21yr-slice) vs $163k(clean) double-take AND the earlier 26.5 vs 31.3 one. NEVER slice recent from the 21yr file.

## 📇 CLEAN RECENT NUMBERS (from recent_tier_curves.json, data through Jul 9 2026): trailing-12mo (Jul9'25→Jul9'26, $100k): Preserver +25.8%/$126k, Maximizer +63.5%/$163k, S&P +20.7%/$121k. Maximizer best 12mo window = +85%/$185k (yr ending May'26); does NOT cleanly hit 100% in clean run. Jul1→Jul1: Pres +31%/Max +68%/$168k.

## 🎁 DAD CARD (private, not public — lighter framing OK): /Users/erikkins/Desktop/rigacap-maximizer-card.png ($163,000, Georgia serif brand-fallback since no Fraunces TTF local) + rigacap-year-table.html (Preserver/Maximizer/S&P table). Both consistent on $163k.

## ✅ LAUNCH SEQUENCE SHIPPED: Card #1 POSTED to X(x.com/rigacap/status/2075632566514667745)+Threads+IG. Cards 2-5 SCHEDULED Mon-Thu Jul13-16 @9amET(13:00 UTC) on X/IG/Threads (12 SocialPosts status=scheduled in prod DB; scheduler fires them; editable in Social tab). Threads FLIPPED BACK ON (toggle now X/IG/Threads ON, tiktok off). Posting mechanism: create SocialPost(platform, text_content, hashtags="", image_s3_key=https://rigacap.com/launch-cards/launch-N.png, status) → social_posting_service.publish_post (local, Lambda-env creds). Plain links in caption → _inject_utm vanitizes per-platform at publish. WIDGET (tier-aware /app) PARKED.

## 🎨 PROFILE ASSETS IN FLIGHT: (1) X BANNER rebuilt sharp 1500x500 paper/claret, tagline "Built so you never get a reason to panic-sell" + "One engine two settings—Preserve to Maximize", NO stale numbers → /Users/erikkins/Desktop/rigacap-x-banner.png (source design/brand/profiles/x-banner.png is PNG-only, no HTML). NEEDS TWEAK: avatar overlaps bottom-left (hides sub-line) + mobile crops top/bottom → pull block to vertical center, shift sub up-right. Offered to fix, awaiting. (2) PROFILE BIOS drafted (X≤160/IG≤150/Threads/LinkedIn) — Erik pastes (no bio API). (3) LinkedIn ABOUT rewrite OWED: Erik pasted old one (single-product, "six positions" STALE→now ~20 pos, "friction-adjusted"→walk-forward, add 2-tier Preserver/Maximizer, keep discipline/do-nothing + founder line). Structure: intro/approach/audience/how-it-works/founded.

## 📣 GOOGLE ADS REWORK — IN FLIGHT (Erik pulled Overview-cards CSV export to /Users/erikkins/Downloads/Overview_cards_csv(2026-07-10_13_57_37)). ANALYZED:
- STRUCTURE: 1 campaign `stability-search-test-a` (Enabled, $695/134clk/3.2%CTR, Jun12-Jul9). 2 ad groups: `crash-protection` ($555/110clk) + `panic-discipline` ($140/24clk). ALL keywords DEFENSIVE (protect/crash/panic/volatile) — no growth/Maximize intent yet.
- 🚨 #1 FINDING: **ZERO conversions on $695 / 134 clicks** (every keyword+search-term row = 0.00). = tracking gap (GA4→Ads import — most likely, flagged before) OR land→signup leak. FIX THIS FIRST — asked Erik: is sign_up/purchase IMPORTED as an Ads conversion action, or only GA4 key-events? Don't optimize spend while blind.
- BEST CTR kw (lean in): protect portfolio from crash 5.67%, prepare portfolio for recession 5.56%, reduce stock market risk 4.96%. WORST: `stock market too volatile` = #1 SPENDER $147 @ weak 2.55% → pulling stock-SCREENER queries (most volatile stocks today / high volatility stocks / volatile stocks today) = wrong intent. ADD NEGATIVES: "most volatile stocks","high volatility stocks","volatile stocks today". trailing stop strategy = low 1.64% CTR.
- AUDIENCE (confirms Preserve persona): 60% are 55+, 77% male, 80% mobile. Fisher Investments outranks (69% top-of-page vs your 46%; you = 12% impr share). Competitors: fisher/seekingalpha/warriortrading/thebalancemoney.
- REWORK PLAN told Erik: (1) fix/confirm conversion tracking FIRST, (2) ADD "Maximize" ad group + growth keywords (new territory), (3) add the 3 negatives, (4) swap in new 2-tier RSA copy (DRAFTED both groups Preserve+Maximize, policy-safe/no flat return claims — in transcript). MISSING from export: current live AD COPY (headlines/desc) — asked Erik to screenshot a running ad OR just swap wholesale. AWAITING: Erik answer on conversion-tracking + copy-swap choice.
- API access still needs Erik's mgr acct + Google-approved dev token (days-wks) — deferred, manual for now.

## KEY DECISIONS: verification (not link-removal) fixed Meta ban; links OK on verified acct — only the UTM funnel string was the risk. Toggle live: IG/Threads OFF, X/TikTok ON (S3 social/platform_toggles.json). Meta account VERIFIED as RigaCap LLC.
## ⭐ RULES: no Core/t30v public; walk-forward not backtest; no tildes; claret+paper only; NEVER `aws lambda --environment` partial; commit/push only when Erik asks. Untracked (exclude): scripts/shapes_tpe.db, .claude/.memory_checkpoint_ts.
